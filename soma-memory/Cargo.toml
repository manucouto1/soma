[package]
name = "soma-memory"
version.workspace = true
edition.workspace = true
readme = "../README.md"
license.workspace = true
repository.workspace = true
description = "Knowledge base and temporal experiment tracking for the Soma runtime"

[dependencies]
soma-core = { version = "0.2.5", path = "../soma-core" }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
thiserror = { workspace = true }
chronos-vector = { version = "0.1", optional = true }

[dev-dependencies]
proptest = { workspace = true }
chronos-vector = "0.1"

[features]
default = []
chronos = ["dep:chronos-vector"]
