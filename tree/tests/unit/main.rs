//! One test binary, one `mod` per module, as in soma-next.

//! One test binary, one `mod` per module, as in soma-next.
mod data;
mod findings;
mod journal;
mod moves;
mod revision;
mod snapshot;
mod trials;
mod walk;
