[package]
name = "soma-next-study"
version.workspace = true
edition.workspace = true
license.workspace = true

# No dependencies, and not even the core's: a partition is arithmetic over
# indices. It does not know what a graph is, and nothing here needs it to.

[[test]]
name = "unit"
path = "tests/unit/main.rs"
