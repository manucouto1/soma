[workspace]
members = ["core", "python", "store", "transport"]
resolver = "3"

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "Elastic-2.0"
repository = "https://github.com/manucouto1/soma-next"

[workspace.dependencies]
soma-next-core = { path = "core" }
soma-next-store = { path = "store" }
serde = { version = "1", features = ["derive"] }
