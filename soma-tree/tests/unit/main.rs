//! One test binary, one `mod` per module, as in soma.

//! One test binary, one `mod` per module, as in soma.
mod data;
mod findings;
mod journal;
mod moves;
mod revision;
mod snapshot;
mod trials;
mod walk;
