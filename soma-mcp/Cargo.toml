[package]
name = "soma-mcp"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "MCP (Model Context Protocol) server for Soma research platform"

[[bin]]
name = "soma-mcp"
path = "src/main.rs"

[dependencies]
soma-core = { path = "../soma-core" }
soma-compiler = { path = "../soma-compiler" }
soma-runtime = { path = "../soma-runtime" }
soma-memory = { path = "../soma-memory" }
soma-agent = { path = "../soma-agent" }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
chrono = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
