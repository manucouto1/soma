[package]
name = "soma-agent"
version.workspace = true
edition.workspace = true
readme = "../README.md"
license.workspace = true
repository.workspace = true
description = "Autonomous research agent for the Soma runtime"

[dependencies]
soma-core = { version = "0.2.5", path = "../soma-core" }
soma-runtime = { version = "0.2.5", path = "../soma-runtime" }
soma-memory = { version = "0.2.5", path = "../soma-memory" }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
