// @ts-check
import { defineConfig } from 'astro/config';
import starlight from '@astrojs/starlight';
import { remarkMermaid } from './src/plugins/remark-mermaid.mjs';

// GitHub Pages project site: https://manucouto1.github.io/soma/
// `base` must be set or every internal link resolves to the domain
// root and 404s once deployed. `astro dev` also serves under the base,
// so links that include it work identically in dev and in production.
const SITE = 'https://manucouto1.github.io';
const BASE = '/soma';

// https://astro.build/config
export default defineConfig({
	site: SITE,
	base: BASE,
	// ```mermaid fences become `<pre class="mermaid">`, which Shiki leaves
	// alone and src/components/Head.astro renders in the browser.
	markdown: { remarkPlugins: [remarkMermaid] },
	integrations: [
		starlight({
			title: 'Soma',
			// The default highlighting was the loudest thing on any page: a
			// saturated purple keyword and an orange string, against a ground
			// chosen to be looked at for a long time. These two are the same
			// grammar dimmed, and the surface is pinned to the site's own
			// tokens so a code block is a level of this palette rather than a
			// rectangle from somebody else's.
			expressiveCode: {
				themes: ['github-dark-dimmed', 'github-light'],
				styleOverrides: {
					codeBackground: 'var(--sl-color-gray-7)',
					borderColor: 'var(--sl-color-gray-5)',
					borderRadius: '0.5rem',
					codeFontSize: 'var(--sl-text-code)',
					frames: {
						editorTabBarBackground: 'var(--sl-color-gray-6)',
						editorActiveTabBackground: 'var(--sl-color-gray-7)',
						terminalBackground: 'var(--sl-color-gray-7)',
						terminalTitlebarBackground: 'var(--sl-color-gray-6)',
						shadowColor: 'transparent',
					},
				},
			},
			// Notebook outputs bring their own baked-in colours; see the file.
			customCss: [
				'./src/styles/theme.css',
				'./src/styles/layout.css',
				'./src/styles/notebooks.css',
				'./src/styles/diagrams.css',
			],
			components: { Head: './src/components/Head.astro' },
			description: 'A computational graph you declare, run across machines, and can ask what it did.',
			// Two files rather than one with a `prefers-color-scheme`
			// block: Starlight renders the logo as an <img>, so a media
			// query inside the SVG would follow the OS and ignore the
			// site's own theme toggle. Starlight swaps these with CSS.
			logo: {
				light: './src/assets/logo-light.svg',
				dark: './src/assets/logo-dark.svg',
			},
			favicon: '/favicon.svg',
			head: [
				// Rasterized: X and several others refuse SVG previews.
				{ tag: 'meta', attrs: { property: 'og:image', content: `${SITE}${BASE}/og-card.png` } },
				{ tag: 'meta', attrs: { name: 'twitter:card', content: 'summary_large_image' } },
				{ tag: 'meta', attrs: { name: 'twitter:image', content: `${SITE}${BASE}/og-card.png` } },
			],
			social: [{ icon: 'github', label: 'GitHub', href: 'https://github.com/manucouto1/soma' }],
			defaultLocale: 'en',
			sidebar: [
				{
					// What somebody reads before deciding whether this is for them. The
					// two things that make it different — a search that hands trials out
					// of a folder, and an investigation versioned beside the code — are
					// tours here and have their depth in their own groups below.
					label: 'Start here',
					items: [
						{ label: 'What this is', slug: 'start/what-this-is' },
						{ label: 'Install, and a first graph', slug: 'start/quickstart' },
						{ label: 'The notebooks', slug: 'start/notebooks' },
					],
				},
				{
					// Generated from examples/ by scripts/notebooks_to_docs.py (an npm
					// pre-hook), so these pages are not in git. Listed one by one rather
					// than autogenerated: check-sidebar.mjs reads `slug:` literals, and a
					// new notebook SHOULD fail that guard until someone gives it a place.
					label: 'Tutorials',
					collapsed: true,
					items: [
						{ label: '01 — Declaring a graph', slug: 'tutorials/01-declaring-a-graph' },
						{ label: '02 — Watching a run', slug: 'tutorials/02-watching-a-run' },
						{ label: '03 — Training', slug: 'tutorials/03-training' },
						{ label: '04 — A study', slug: 'tutorials/04-a-study' },
						{ label: '05 — The health of a network', slug: 'tutorials/05-the-health-of-a-network' },
						{ label: '06 — A problem, end to end', slug: 'tutorials/06-a-problem-end-to-end' },
						{ label: '07 — A real architecture', slug: 'tutorials/07-a-real-architecture' },
						{ label: '08 — Before a step is taken', slug: 'tutorials/08-before-a-step-is-taken' },
						{ label: '09 — A fleet', slug: 'tutorials/09-a-fleet' },
						{ label: '10 — Where the data comes from', slug: 'tutorials/10-a-dataset' },
						{ label: '11 — What an edit did', slug: 'tutorials/11-what-an-edit-did' },
						{ label: '12 — Where a value came from', slug: 'tutorials/12-where-a-value-came-from' },
						{ label: '13 — The reasoning of an investigation', slug: 'tutorials/13-the-reasoning-of-an-investigation' },
					],
				},
				{
					// The vocabulary. Five facts that are orthogonal, five holes the core
					// provides and does not fill, three levels none of which knows the one
					// above exists. Confusing any of them is the easy mistake, which is
					// why they are a group and not a paragraph in an overview.
					label: 'The model',
					items: [
						{ label: 'Overview', slug: 'model/overview' },
						{ label: 'A node is one thing', slug: 'model/a-node' },
						{ label: 'The rules it is written under', slug: 'model/philosophy' },
					],
				},
				{
					label: 'Running one',
					items: [
						{ label: 'The plan: what runs when', slug: 'running/the-plan' },
						{ label: 'Across machines', slug: 'running/across-machines' },
						{ label: 'What is remembered', slug: 'running/what-is-remembered' },
						{ label: 'Where the data comes from', slug: 'running/where-the-data-comes-from' },
						{ label: 'The store', slug: 'running/the-store' },
						{ label: 'Training', slug: 'running/training' },
					],
				},
				{
					// A third of this framework. The declaration drawn, the record of what
					// happened, and the diagnosis — which is an opinion about the record
					// and not a fact in it. Keeping them apart is what makes "a diagnosis
					// has to be reproducible from the stored record" a test.
					label: 'Looking at it',
					items: [
						{ label: 'The graph, drawn', slug: 'looking/the-graph-drawn' },
						{ label: 'The record of what happened', slug: 'looking/the-record' },
						{ label: 'The health of a network', slug: 'looking/health' },
					],
				},
				{
					label: 'Searching',
					items: [
						{ label: 'A study', slug: 'searching/a-study' },
						{ label: 'Handing trials out of a folder', slug: 'searching/handing-out-trials' },
					],
				},
				{
					// No Python page yet, and that hole is deliberate. The one legacy
					// carried documented `Filter`, `DifferentiableFilter` and `board` —
					// an API this framework does not have — and a reference that is
					// wrong is worse than one that is missing, because it is the page
					// people copy from.
					label: 'Reference',
					items: [
						{
							// cargo-doc output, copied into dist/api/rust/ by the deploy
							// workflow — a raw link, so it carries the base explicitly
							// (slug: entries get it for free). It used to sit at the site
							// root and this site was what it displaced.
							label: 'Rust API (cargo doc)',
							link: `${BASE}/api/rust/`,
							attrs: { target: '_blank' },
						},
						{ label: 'The crates', slug: 'reference/crates' },
					],
				},
				{
					label: 'The project',
					items: [
						{
							// The only autogenerated group, and check-sidebar knows the word.
							// These are the 38 sections of docs/use-cases.md, the document the
							// project is written from: one page per slice, rendered by an npm
							// pre-hook and not in git. Hand-listing them would mean editing
							// this file every time a slice closes.
							label: 'Use cases',
							collapsed: true,
							autogenerate: { directory: 'use-cases' },
						},
						{ label: 'Contributing', slug: 'project/contributing' },
						{ label: 'How it is tested', slug: 'project/how-it-is-tested' },
					],
				},
			],
		}),
	],
});
