{
  "version": "1.0.0",
  "modules": [
    {
      "name": "somatize",
      "doc": "soma: the re-derived twin of Soma, one use case at a time.\n\nA node is anything with a `forward`. It takes what arrived along the edges and\nreturns what it produced — there is no wrapper around either::\n\n    from somatize import Graph, Node\n\n    class Clean(Node):\n        def forward(self, x, ctx):\n            return x.strip()\n\n    class Shout(Node):\n        def forward(self, x, ctx):\n            return x.upper()\n\n    g = Graph.somatize(Clean() >> Shout())\n    g.forward(\"  hello  \")\n\nWhatever a node takes to answer — a retry, a model, three rounds of something —\nhappens **inside it**. `Graph()` with `node()` and `edge()` is still there for\nwhen the topology is built in a loop or comes from outside.\n\n`codec(kind, type, dump=..., load=...)` says how something wrapped in `Opaque` is\nwritten down, which is what lets a graph **keep** what it produces.\n\n`Store(directory)` is that same place, opened by hand. A directory two machines\ncan both see is how a training run written down on one is read back on another::\n\n    store.keep(\"round/3\", trainer.export())\n    trainer.load(store.recall(\"round/3\"))",
      "classes": [
        {
          "name": "Bound",
          "signature": null,
          "doc": "A name, and what it points at.",
          "exception": false,
          "constructors": [],
          "methods": [],
          "properties": [
            {
              "name": "digest",
              "doc": "The digest of the bytes it points at."
            },
            {
              "name": "meta",
              "doc": "What was said beside it, in the order it was said."
            },
            {
              "name": "name",
              "doc": "The name somebody chose."
            },
            {
              "name": "when",
              "doc": "When it was bound, in seconds since the epoch."
            }
          ]
        },
        {
          "name": "Ctx",
          "signature": "(device=None)",
          "doc": "What a node knows beyond its input.",
          "exception": false,
          "constructors": [],
          "methods": [],
          "properties": [
            {
              "name": "device",
              "doc": "Where this node was said to run — `\"cuda:0\"` — or `None`. Written the\nway torch writes it, so it can be handed straight to `.to()`."
            }
          ]
        },
        {
          "name": "Graph",
          "signature": "()",
          "doc": "A computation graph: nodes, edges and what each one executes.\n\nEverything else — `node`, `edge`, `plan` and the topology queries — is\ninherited from the Rust class.",
          "exception": false,
          "constructors": [
            {
              "name": "somatize",
              "signature": "(topology: _dsl.Topology) -> Graph",
              "doc": "Materializes an expression into an executable graph.\n\nYou think it, Soma somatizes it::\n\n    Graph.somatize(Source() >> (Left() | Right()) >> Mean())"
            }
          ],
          "methods": [
            {
              "name": "cache",
              "signature": "(node_id, salt=None)",
              "doc": "Says this node's output is worth keeping, with the salt that tells apart\ntwo runs the key cannot tell apart on its own."
            },
            {
              "name": "cached",
              "signature": "()",
              "doc": "Which nodes are kept, and under what salt."
            },
            {
              "name": "declarations",
              "signature": "()",
              "doc": "What each node was built with, digested, for those where it could be\nsaid."
            },
            {
              "name": "declared_as",
              "signature": "(node_id, declaration)",
              "doc": "Notes what this node was built with, digested. **In the key**, beside\nthe identity: `Embed(512)` and `Embed(64)` are one class and two answers."
            },
            {
              "name": "devices",
              "signature": "()",
              "doc": "Where each placed node runs, in declaration order."
            },
            {
              "name": "edge",
              "signature": "(source, target)",
              "doc": "Connects two nodes. Both have to exist already."
            },
            {
              "name": "edges",
              "signature": "()",
              "doc": "The edges as `(source, target)` pairs, in insertion order."
            },
            {
              "name": "figure",
              "signature": "(overlay: Overlay | None = None, inside: Inside | None = None) -> Figure",
              "doc": "The graph drawn, as a `plotly.graph_objects.Figure`. Nothing is\nexecuted to draw it. Needs the `viz` extra.\n\n`inside` opens a node up — `{node: [(path, what), ...]}`, which\n`somatize.torch.architecture` reads off the modules it holds. `overlay`\nlays what **happened** over what was declared, `{node: [flag, ...]}`."
            },
            {
              "name": "fingerprints",
              "signature": "()",
              "doc": "Which version of the code each node was written against, for those\nwhere it was noted."
            },
            {
              "name": "foreseen_json",
              "signature": "(input=None, *, store=None)",
              "doc": "What each node's output will be called, with **nothing executed**.\n\nThe pass `forward` makes before its first node, asked for on its own:\n`{\"keys\": {node: name}, \"unneeded\": [node, ...]}`, where `unneeded` is\nwhat would not have to run because something below it is already kept.\n\n`store=` for the same reason `forward` takes one — without a keeper\nnothing is named. A directory nothing was written to answers fine.\n\nTwo kinds of node are missing from `keys` and it is not an omission: a\n`.mapped()` one, and anything under an input that cannot be written down.\nRead the absence as *cannot tell*, never as *did not change*. Both parts\nare ordered by id, because this crosses a process boundary."
            },
            {
              "name": "forward",
              "signature": "(input: Any | None = None, *, broker: Broker | None = None, store: Store | str | None = None, watching: Callable[[Fact], None] | list[Callable[[Fact], None]] | None = None, stamping: dict[str, str] | None = None) -> Any",
              "doc": "Executes the whole graph and returns what it produced.\n\n`broker=Broker.embedded({\"w1\": Worker.at(...)})` says who knows where\neach host is. **This method sends the nodes**, not you.\n\n`store` is a directory or a `Store`: with one, whatever was declared\n`.cached()` is looked up before being computed and kept after.\n\n`watching` is told what happened, as it happens::\n\n    g.forward(x, watching=print)                    # in a notebook\n    g.forward(x, watching=Recorder(store))          # kept\n    g.forward(x, watching=[Recorder(store), draw])  # both\n\nA fact arrives as a `dict` with a `fact` key naming it — the same shape\nit is written down as. What a worker saw comes back down the connection\nthat was already open."
            },
            {
              "name": "freeze",
              "signature": "(node_id, state=None)",
              "doc": "Says this node's state does not change from here on, with the digest of\nthe state it is settled at if the caller knows how to hash weights. The\nprimitive `.frozen()` ends at, and it **declares**: making it true is\n`somatize.torch.freeze`."
            },
            {
              "name": "frozen",
              "signature": "()",
              "doc": "Which nodes are settled, and at what state — `None` for one with none."
            },
            {
              "name": "hosts",
              "signature": "()",
              "doc": "Which host each node sent away runs on, in declaration order."
            },
            {
              "name": "identities",
              "signature": "()",
              "doc": "What implements each node, by name."
            },
            {
              "name": "implementation",
              "signature": "(node_id)",
              "doc": "The object you registered under `node_id`, or `None`."
            },
            {
              "name": "leaves",
              "signature": "()",
              "doc": "The nodes where it leaves."
            },
            {
              "name": "mapped",
              "signature": "(node_id)",
              "doc": "Says this node maps over the items of its input: a list in, a list as\nlong out. What it buys is a cache with the grain of an **item**."
            },
            {
              "name": "mapped_nodes",
              "signature": "()",
              "doc": "Which nodes map over the items of their input, in declaration order. A\nlist and not a dict, since mapping carries nothing beside it — and\n`mapped_nodes` rather than `mapped`, which is already the setter."
            },
            {
              "name": "node",
              "signature": "(*args: Any) -> str",
              "doc": "Adds a node and returns its id, noting **what it was built with**.\n\nHere and not in `_dsl`, because a graph built by hand in a loop has the\nsame collision: `Embed(512)` and `Embed(64)` are one class and two\nanswers. What cannot be written down the same way in two processes is\npassed over here and refused in `_check_it_was_obeyed`."
            },
            {
              "name": "nodes",
              "signature": "()",
              "doc": "The ids, in insertion order."
            },
            {
              "name": "place",
              "signature": "(node_id, device)",
              "doc": "Places a node on a device. The primitive the DSL's `.on()` ends up\ncalling, and what you use when you only have the id."
            },
            {
              "name": "place_at",
              "signature": "(node_id, host)",
              "doc": "Sends a node to a host, by name: the other half of `place`, independent\nof it. What the name resolves to is decided by whoever executes, in\n`forward(broker=…)`."
            },
            {
              "name": "plan",
              "signature": "()",
              "doc": "How this graph will be walked: the decided shape, already distributed.\nWith no host set, `distribute` changes nothing."
            },
            {
              "name": "plan_json",
              "signature": "()",
              "doc": "The same shape, as **data**: `Plan`'s own serde form, as JSON text.\n\nTwo methods because they answer to different readers: `plan()` is for a\nperson, and this one is for whoever draws it — parsing a `Debug` to find\nout what runs beside what is how a renderer starts lying."
            },
            {
              "name": "predecessors",
              "signature": "(node_id)",
              "doc": "The nodes feeding into `node_id`."
            },
            {
              "name": "provision",
              "signature": "(broker: Broker | dict[str, Any] | None) -> None",
              "doc": "Tells each host what it is going to need, before the first node runs.\n\n`forward` calls it, and whoever runs a graph **in pieces** does not have\nto either: a piece provisions the graph it is a piece of, entire. That is\nwhy the method exists — a worker has **one** catalog, and half of one is\na different catalog, refused mid-session and swallowed in silence by a\nworker that has not greeted yet.\n\nA host that gets nothing is told nothing, and **two hosts that turn out\nto be one place are told once**, with the union of what they hold."
            },
            {
              "name": "roots",
              "signature": "()",
              "doc": "The nodes where execution enters."
            },
            {
              "name": "successors",
              "signature": "(node_id)",
              "doc": "The nodes `node_id` feeds into."
            },
            {
              "name": "topological_sort",
              "signature": "()",
              "doc": "The nodes in an order where each comes after its predecessors."
            },
            {
              "name": "written_as",
              "signature": "(node_id, fingerprint)",
              "doc": "Notes which version of the code this graph was written against.\n**Metadata**: never in a key, compared on a hit and said on `stderr` if\nit differs."
            }
          ],
          "properties": []
        },
        {
          "name": "Node",
          "signature": "()",
          "doc": "What a graph node executes. `forward` has to be written or the class\ncannot be instantiated.",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "at",
              "signature": "(host: str) -> Piece",
              "doc": "The same piece, in another process. The innermost one wins, and\n**independently** of `.on()`, so the two can be written in any order. A\nhost is a **name**: what it resolves to is said by whoever executes."
            },
            {
              "name": "cached",
              "signature": "(salt: str | None = None) -> Piece",
              "doc": "The same piece, worth keeping: what each of its nodes produces is\nlooked up before being computed, and kept after.\n\nOpt-in, because keeping costs — and a node without it **does not break\nthe chain**: its key is still computed and passed on. `salt` tells apart\ntwo runs the key cannot. Not in the key: the device, nor the fingerprint."
            },
            {
              "name": "forward",
              "signature": "(input: Any, ctx: Ctx) -> Any",
              "doc": "Runs it: takes what arrived along the edges, returns what it made.\n\n`ctx` carries `device`, which is where this node was told to run."
            },
            {
              "name": "frozen",
              "signature": "() -> Piece",
              "doc": "The same piece, settled: its state does not change while the graph\nruns. Here it is **declared**; making it true is `somatize.torch.freeze`,\nthe same division as `.on()`, where the core says where and the node\nmoves itself."
            },
            {
              "name": "mapped",
              "signature": "() -> Piece",
              "doc": "The same piece, mapping over the items of its input: a list in, a list\nas long out, item for item.\n\nWhat gives a cache the grain of an **item** — without it, one new\ndocument among a thousand makes all thousand miss. The node is handed\n**only the items that are missing**, so it still batches, and an item is\nnamed after **itself** rather than after its position."
            },
            {
              "name": "named",
              "signature": "(node_id: str) -> Declared",
              "doc": "The same node, with the id you say."
            },
            {
              "name": "on",
              "signature": "(device: str) -> Piece",
              "doc": "The same piece, placed on a device. **The innermost one wins**, so\n``(A().on(\"cuda:0\") >> B()).on(\"cuda:1\")`` leaves `A` on 0 and `B` on 1.\n\nThe name is validated when the graph is materialized, in Rust."
            }
          ],
          "properties": []
        },
        {
          "name": "Opaque",
          "signature": "(value)",
          "doc": "Marks a value so it crosses the graph untouched. The node that receives it\nsees it **unwrapped**, so it is only written on returning.",
          "exception": false,
          "constructors": [],
          "methods": [],
          "properties": [
            {
              "name": "value",
              "doc": "The object as it is."
            }
          ]
        },
        {
          "name": "Recorder",
          "signature": "(store, *, run=None, summarising=None)",
          "doc": "Writes down what happened, one record per `forward`.\n\n```python\nr = Recorder(store)                 # or Recorder(store, run=\"tuesday\")\ng.forward(x, watching=r)\nstore.resolve(f\"run/{r.run}/0\")     # what that forward did\n```",
          "exception": false,
          "constructors": [],
          "methods": [],
          "properties": [
            {
              "name": "run",
              "doc": "What this run is called, which is the first half of every name it writes."
            }
          ]
        },
        {
          "name": "Store",
          "signature": "(where_)",
          "doc": "Something that keeps bytes by their content, and names that point at them.\nA `dyn Store` and not a `Local`, since there are two — and which one this is\ndoes not reach the rest of Python, which is what lets a study run over a\nshared folder here and over S3 there without a line changing.",
          "exception": false,
          "constructors": [
            {
              "name": "on_bucket",
              "signature": "(endpoint, bucket, *, region='us-east-1', key=None, secret=None, hosted=False)",
              "doc": "The same store, on a bucket: S3, MinIO, R2 — for a cluster with no shared\ndirectory. `hosted=True` puts the bucket in the host name, which recent\nAWS wants; the default puts it in the path, which MinIO wants. Without\n`key`/`secret` it reads `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY`.\n\n**It talks to the endpoint before returning**: one that takes a\nconditional write and writes anyway would hand every trial to every\nmachine and never say so."
            }
          ],
          "methods": [
            {
              "name": "bind",
              "signature": "(name, digest, meta=None)",
              "doc": "Points a name at some bytes, with whatever you want to remember beside\nit. Binding the same name again replaces it."
            },
            {
              "name": "bound",
              "signature": "()",
              "doc": "Everything bound here. A scan, and that is the point: the records are the\ntruth and an index over them is something you can throw away."
            },
            {
              "name": "claim",
              "signature": "(name, digest, meta=None)",
              "doc": "Points a name at some bytes **only if nobody has**, and says whether it\ndid. This is how work gets handed out — not `resolve` then `bind`, since\nbetween the two somebody else does the same::\n\n    me = store.put(f\"{socket.gethostname()}/{os.getpid()}\".encode())\n    if store.claim(f\"round/{r}/client/{k}\", me):\n        ..."
            },
            {
              "name": "get",
              "signature": "(digest)",
              "doc": "The bytes, or `None` if this store does not have them."
            },
            {
              "name": "keep",
              "signature": "(name, what, meta=None)",
              "doc": "Keeps a value under a name — tensors and all, by the codecs. The two\nlines that make an export cross a machine::\n\n    store.keep(\"round/3\", trainer.export())\n    trainer.load(store.recall(\"round/3\"))\n\nWhat reaches the directory is bytes, and it never learns any of it was a\ntensor. Something nobody registered a codec for is refused with its type."
            },
            {
              "name": "put",
              "signature": "(bytes)",
              "doc": "Saves these bytes and gives back the digest that names them.\n\nSaving the same bytes twice is saving them once: that is what content\naddressing is for, and it is why a round of federated training that\nchanged nothing costs nothing."
            },
            {
              "name": "recall",
              "signature": "(name)",
              "doc": "What is kept under that name, alive again, or `None` if nothing is."
            },
            {
              "name": "resolve",
              "signature": "(name)",
              "doc": "What that name points at, or `None`."
            }
          ],
          "properties": []
        },
        {
          "name": "Broker",
          "signature": "(listing)",
          "doc": "Where the hosts of a graph are, and who resolves them. One deployment\ntoday — the one inside this process, which is what makes soma work with no\nplatform and no head node::\n\n    g.forward(x, broker=Broker.embedded({\"w1\": Worker.at(\"node3:7000\")}))\n\nThe others speak the same protocol, so what changes for a client is a URL.",
          "exception": false,
          "constructors": [
            {
              "name": "embedded",
              "signature": "(workers: dict[str, Worker]) -> Broker",
              "doc": "A broker inside this process, knowing where these hosts are."
            }
          ],
          "methods": [
            {
              "name": "packing_for",
              "signature": "(host: str) -> Worker",
              "doc": "The worker declared for this host."
            },
            {
              "name": "provision",
              "signature": "(host, kind, id, blob, runtime)",
              "doc": "Tells the host's wire what to provision the far side with, before the\nfirst job. Staged: nothing is sent until somebody dispatches, and even\nthen only if the far side asks for it."
            },
            {
              "name": "token_for",
              "signature": "(host: str) -> bytes | None",
              "doc": "What this host shares a wire with, or `None` if the broker does not\nknow it.\n\n`None` rather than an exception because a graph may name a host nobody\nlisted: either the run reaches it or it does not, and whichever happens\nsays so with the slice in front of it."
            },
            {
              "name": "wire_token",
              "signature": "(host)",
              "doc": "Bytes that are equal for two hosts that share a wire, so whoever decides\nwhat to pack can group by them. **Asking is eager**, because what gets\npacked depends on which hosts are the same place; connecting is not."
            }
          ],
          "properties": []
        },
        {
          "name": "Worker",
          "signature": "(target: str | list[str], mode: str = 'project', send: Sequence[str] = ())",
          "doc": "Where a slice goes, and how to pack for it. It opens nothing.\n\nA declaration and not a connection, which is the change a broker brings::\n\n    python -m somatize.worker --listen 0.0.0.0:7000     # on the other machine\n    g.forward(x, broker=Broker.embedded({\"w1\": Worker.at(\"node3:7000\")}))\n\n`mode` says what gets sent: `\"project\"` *(default)* sends names, versions and\nstate and the worker supplies the code from its clone; `\"network\"` sends the\ncode too, and `send=[\"my_package\"]` makes your own modules travel inside it.\nBecause it declares rather than connects, **a host that is not there fails\nwhen it is needed rather than when it is named**.",
          "exception": false,
          "constructors": [
            {
              "name": "at",
              "signature": "(addr: str, mode: str = 'project', send: Sequence[str] = ()) -> Worker",
              "doc": "A worker that **is already standing** somewhere."
            },
            {
              "name": "generic",
              "signature": "(mode: str = 'project', send: Sequence[str] = (), python: str | None = None) -> Worker",
              "doc": "A child running `python -m somatize.worker`."
            },
            {
              "name": "spawn",
              "signature": "(argv: list[str], mode: str = 'project', send: Sequence[str] = ()) -> Worker",
              "doc": "A worker to be started as a child process. For testing: while the\nclient starts the process, there is no independent worker worth the\nname."
            }
          ],
          "methods": [
            {
              "name": "packed",
              "signature": "(nodes: dict[str, Any]) -> tuple[str, str, bytes]",
              "doc": "These nodes as an artifact the way this worker wants them: its kind,\nits id, and its bytes."
            }
          ],
          "properties": []
        }
      ],
      "functions": [
        {
          "name": "codec",
          "signature": "(kind, of_type, *, dump, load)",
          "doc": "Says how objects of a type are written down and read back.\n\n`dump(obj) -> bytes` and `load(bytes) -> obj`. The `kind` is what gets\nwritten beside the bytes, so it is what a store keeps forever: name it after\nthe type, not after the run."
        },
        {
          "name": "codecs_registered",
          "signature": "()",
          "doc": "What has a codec registered today, in the order they were registered."
        }
      ],
      "constants": [
        {
          "name": "__version__",
          "type": "str",
          "repr": "'1.0.0'"
        }
      ]
    },
    {
      "name": "somatize.data",
      "doc": "Where the data comes from, and whether the model is learning what you meant.\n\nTwo halves that meet on the same word. **Where it comes from**::\n\n    from somatize import Graph, Store\n    from somatize.data import Parquet, settle, to_polars\n\n    sms = Parquet(Store(\"/data\"), \"sms/train\")\n    g = Graph.somatize(sms.named(\"sms\").frozen() >> Clean().named(\"clean\").cached())\n    settle(g)\n    g.forward({\"at\": 0, \"take\": 64}, store=\"/data\")\n\nA source is a node, what it answers with is Arrow, and what the graph is handed\nis a **coordinate** — which is what stops a cache weighing the batch on every\nstep. See `somatize.data._source`.\n\nAnd **whether it is learning what you meant**, which is not about the network at\nall::\n\n    from somatize.data import contribution, leaning, shares\n\n    said = contribution(g, batches, objective=mse, over=(\"symptoms\", \"text\"))\n    shares(said)     # {\"symptoms\": 0.01, \"text\": 0.99}\n    leaning(said)    # {\"symptoms\": [\"IGNORED_INPUT(symptoms)\"], ...}\n\n`somatize.health` asks whether a network is **learning**: gradients,\nactivations, channels, the update. This asks whether it is learning **what you\nmeant** — which no amount of looking at a gradient will ever say.\n\nIt exists because of a real project: symptom channels for detecting a\nmental-health condition, where interpretability and performance could be had one\nat a time and never together. Months went into the architecture. The signal was\nin the self-disclosure and not in the symptoms, and one afternoon of taking\ninputs away would have said so.",
      "classes": [
        {
          "name": "Parquet",
          "signature": "(store: Store, name: str)",
          "doc": "A parquet file in a store, answering spans of rows.\n\n`Parquet(store, name)` where `name` is what the file is bound under. It\nresolves the name and reads **nothing**: a graph that names a dataset has not\nopened it.",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "at",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "cached",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "forward",
              "signature": "(input: Any, ctx: Ctx) -> Frame",
              "doc": "The rows that span names, as a `Frame`."
            },
            {
              "name": "frozen",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "mapped",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "named",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "on",
              "inherited_from": "somatize.Node"
            }
          ],
          "properties": [
            {
              "name": "name",
              "doc": "The name it was declared under, which is the graph's word."
            },
            {
              "name": "version",
              "doc": "What this dataset is: the digest of its content, which the store had\nalready worked out. It is the duck `settle` and `somatize.torch.freeze`\nboth look for."
            }
          ]
        }
      ],
      "functions": [
        {
          "name": "contribution",
          "signature": "(graph: Graph, batches: Iterable[Batch], *, objective: Callable[[Any, Any], float], over: Iterable[str] | None = None, repeats: int = 3, seed: int = 0, broker: Broker | None = None) -> dict[str, float]",
          "doc": "How much worse the score gets without each input, as `{name: drop}`.\n\n`batches` is an iterable of `(input, target)` — the shape a `Trainer` takes —\nand `input` is a mapping, because a graph with two branches is fed a map and\nthe keys are what gets taken away. `over` names which to try.\n\n`repeats` is how many shuffles each gets, averaged: three is enough to stop\none unlucky permutation deciding an afternoon's conclusion. Nothing is\ntrained and nothing changed."
        },
        {
          "name": "leaned",
          "signature": "(drops: dict[str, float], *, thresholds: Thresholds | None = None, title: str = 'what it leaned on') -> Figure",
          "doc": "The share each input was worth, biggest first."
        },
        {
          "name": "leaning",
          "signature": "(drops: dict[str, float], thresholds: Thresholds | None = None) -> dict[str, list[str]]",
          "doc": "What is wrong with what the model is leaning on.\n\n`{name: [flag, ...]}`, and a name with nothing wrong is not in it — for the\nsame reason a healthy node is not in a diagnosis: no flags is not a clean\nbill, it is *nothing tripped*."
        },
        {
          "name": "settle",
          "signature": "(graph: Graph, *node_ids: str) -> None",
          "doc": "Says what each declared-frozen source is settled at. The same shape as\n`somatize.torch.freeze`: the graph **declares** that a node's state does not\nchange and whoever knows what is inside makes it true — here by repeating\nwhat the store already knew, so it costs nothing.\n\nWithout it a dataset is not in the key of anything computed from it and two\ndatasets share a name, which the graph refuses before running."
        },
        {
          "name": "shares",
          "signature": "(drops: dict[str, float], thresholds: Thresholds | None = None) -> dict[str, float]",
          "doc": "What each input is worth, as `{name: share}` — the drops divided by what\nthey add up to, so they read as *how much of what matters is this*."
        },
        {
          "name": "shuffled",
          "signature": "(what: Any, order: Sequence[int]) -> Any",
          "doc": "One input, with its rows put in that order. An `Opaque` is unwrapped and\nwrapped again, since shuffling the wrapper rather than the tensor would be a\nquiet way of measuring nothing.\n\nTorch tensors, lists and tuples. Anything else is left alone: shuffling a\nthing whose first axis is not the batch is a different question by accident."
        },
        {
          "name": "to_arrow",
          "signature": "(frame: Frame) -> pyarrow.Table",
          "doc": "That frame as a `pyarrow.Table`."
        },
        {
          "name": "to_polars",
          "signature": "(frame: Frame) -> Any",
          "doc": "That frame as a `polars.DataFrame`.\n\n`Any` and not `polars.DataFrame`, because `polars` is not a dependency of\nthis package and annotating a type nobody here can import would make the\nchecker's answer depend on what happens to be installed."
        }
      ],
      "constants": []
    },
    {
      "name": "somatize.foreseen",
      "doc": "What a graph's nodes will be called before anything runs, and what an edit did::\n\n    from somatize import foreseen\n\n    foreseen.names(g)                        # what each answer will be called\n    foreseen.unneeded(g, x, store=store)     # what would not have to run at all\n    foreseen.changes(before, after)          # what the edit did\n    foreseen.snapshot(g)                     # the same, kept for later\n\nA name is a hash of the **recipe** and not of the data, so only the graph's input\nis hashed by content and from there down they are hashes of hashes. The engine\nalready makes this pass before its first node; asking for it on its own turns\n*is my cache still good?* into a millisecond instead of a run.\n\n`changes` answers `{node: [finding, ...]}` — the shape `somatize.health` uses,\nand for the same reason: what happens to a node is more than one fact.\n\n| finding | what it says |\n|---|---|\n| `CHANGED` | its **shape** moved: another class, other arguments, or who feeds it |\n| `RESETTLED` | it is frozen at another state — other weights, another version |\n| `SALTED` | its salt moved |\n| `DOWNSTREAM` | none of those moved and its name moved anyway |\n| `STALE` | **its name did not move and its code did** |\n| `SUSPECT` | something above it is `STALE` |\n| `ADDED` / `GONE` | it is in one graph and not the other |\n| `UNVERSIONED` | its answer is kept and nobody can say whether its code moved |\n| `UNKNOWN` | it cannot be named on one side or the other |\n\nThe first three are one question split three ways, because two questions get\nasked of one answer: *does my cache still hold* is all three, *did the code\nchange* is `CHANGED` alone — weights belong to a version, they are not one.\n\n`STALE` exists because the fingerprint of the code is deliberately not in the\nkey: editing a `forward` renames nothing, so a diff that only looked at names\nwould answer *nothing changed* to the very edit being asked about. Here it is an\n**opinion and not an invalidation**, and it reaches down as `SUSPECT`.\n`UNKNOWN` must never be read as *unchanged*: a `.mapped()` node is named by items\nnobody has yet.\n\nEach side is a `Graph` or a `snapshot` of one, because two versions of a module\ndo not coexist in an interpreter. Nothing here reads or writes a store.",
      "classes": [],
      "functions": [
        {
          "name": "changes",
          "signature": "(before: Graph | Snapshot, after: Graph | Snapshot, input: Any | None = None, *, store: Store | str | None = None) -> dict[str, list[str]]",
          "doc": "What an edit did, as `{node: [finding, ...]}`. A node with nothing said\nabout it is not in it. `input` and `store` are only used for a side that is\nstill a graph, since a snapshot has been named already."
        },
        {
          "name": "names",
          "signature": "(graph: Graph, input: Any | None = None, *, store: Store | str | None = None) -> dict[str, str]",
          "doc": "What each node's answer will be called — `{node: name}` — with nothing run.\nA node missing from it cannot be named in advance, which is what a\n`.mapped()` node and anything under it are."
        },
        {
          "name": "snapshot",
          "signature": "(graph: Graph, input: Any | None = None, *, store: Store | str | None = None) -> Snapshot",
          "doc": "Everything `changes` reads about a graph, as plain JSON, so a version can\nbe compared against one that no longer exists in this process. Two are\ncomparable when taken with the same `input`, which the default always is."
        },
        {
          "name": "unneeded",
          "signature": "(graph: Graph, input: Any | None = None, *, store: Store | str) -> list[str]",
          "doc": "The nodes that would not have to run at all, because something below them\nis already kept. A `store` and not a temporary one: this is the only question\nhere whose answer depends on what is in it."
        }
      ],
      "constants": [
        {
          "name": "FINDINGS",
          "type": "tuple",
          "repr": "('STALE', 'SUSPECT', 'CHANGED', 'RESETTLED', 'SALTED', 'DOWNSTREAM', 'ADDED', 'GONE', 'UNVERSIONED', 'UNKNOWN')",
          "items": [
            "STALE",
            "SUSPECT",
            "CHANGED",
            "RESETTLED",
            "SALTED",
            "DOWNSTREAM",
            "ADDED",
            "GONE",
            "UNVERSIONED",
            "UNKNOWN"
          ]
        }
      ]
    },
    {
      "name": "somatize.health",
      "doc": "Whether what happened was healthy. **An opinion, and it says so.**\n\n    from somatize.health import diagnose, about\n    from somatize.torch import Audit\n\n    t = Trainer(g, ..., auditing=True, watching=Recorder(store, run=\"tuesday\"))\n    diagnose(store, run=\"tuesday\")           # {\"body\": [\"VANISHING\"]}\n    about(\"VANISHING\")                       # what to do about it\n\nThree things get called observability and they are not the same: the declaration\n**drawn**, the record of what **happened**, and this — a judgement *about* that\nrecord, at thresholds somebody chose. The line between the last two is an\ninvariant, and it is a test:\n\n> A diagnosis has to be reproducible from the stored record, without training\n> again.\n\nSo the verdict lives in a Rust crate with **no dependencies at all** and this\nmodule carries a store to it. Change a bound and ask again.\n\n| flag | what it means |\n|---|---|\n| `NAN` / `INF` | a number stopped being one; nothing below it means anything |\n| `VANISHING` / `EXPLODING` | the parameter gradients are too small to train on, or too big to step on |\n| `DEAD` / `SATURATED` | the output is off, or pinned where the derivative is nothing |\n| `STALLED` / `OVERSTEPPING` | the update is tiny, or enormous, next to the weights it moves |\n| `DEAD_CHANNELS(n)` / `IGNORED_CHANNELS(n)` | part of the width does nothing, or does something nobody asks for |\n| `LEAKAGE` | two groups meant to stay apart carry the same information |\n| `NARROWING` | the update has collapsed into a few directions |\n| `MISSING_NORMALISATION` | the signal grows over a stretch with nothing normalising it |\n| `LOSING_PLASTICITY` | weights growing, rank falling and units going quiet, all at once |\n\n`about(flag)` says what to do about each, beside the thresholds because they are\none opinion.\n\nThe same three functions read a **probe** — `somatize.torch.probe(g, x)`, one\nforward that was recorded and never trained — because a probe writes `run/<id>/0`\nand nothing here ever asked what made a record.\n\n`profile(store, run=...)` is the picture, because vanishing is a **shape over\ndepth** and not a property of a layer; `flags(store, run=...)` is the table. They\nare the only figures here where colour may mean good-or-bad, because they are the\nonly ones drawing opinions.",
      "classes": [
        {
          "name": "Thresholds",
          "signature": "(**over)",
          "doc": "The bounds a verdict is taken at. **The whole of the opinion, and it is\ndata**: change one and the same record answers differently, without\ntraining again.",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "as_dict",
              "signature": "()",
              "doc": "What they are, as a dict — for writing them down beside a diagnosis, so\nthat an alarm from last week can be read with the bounds it was taken\nat."
            }
          ],
          "properties": []
        }
      ],
      "functions": [
        {
          "name": "alerts",
          "signature": "(store: Store, *, run: str, thresholds: Thresholds | None = None, last: int | None = None) -> Alerts",
          "doc": "What is wrong, loudly. See [`Alerts`]."
        },
        {
          "name": "about",
          "signature": "(flag)",
          "doc": "What a flag means and what to do about it, by name.\n\nBeside the flag and not in whoever draws it: the bounds and the advice are\none opinion, and splitting them is how a dashboard ends up saying something\nthis library never said."
        },
        {
          "name": "diagnose",
          "signature": "(store: Store, *, run: str, thresholds: Thresholds | None = None, last: int | None = None) -> dict[str, list[str]]",
          "doc": "What is wrong, as `{where: [flag, ...]}`. `where` is a node, or\n`node.path.to.submodule` when `inside=` was asked to look in.\n\nA node with nothing wrong is **not in the answer**. Empty would say *this was\nchecked and is fine*, and no flags does not mean that: a metric nobody\nmeasured cannot raise one, and `seen` is what says what was measured.\n\nA fetch per `forward` looked at, because the numbers are in the blobs."
        },
        {
          "name": "flags",
          "signature": "(store: Store, *, run: str, thresholds: Thresholds | None = None, last: int | None = None) -> Figure",
          "doc": "What is wrong with each node, and what to do about it. A table and not a\nlist, because a diagnosis without its advice is a word somebody has to look\nup. A node with nothing wrong is not a row: empty would read as *checked and\nfine*, and a metric nobody measured cannot raise a flag."
        },
        {
          "name": "history",
          "signature": "(store: Store, *, run: str, node: str, of: str = 'grad_norm', last: int | None = None) -> list[tuple[int, float]]",
          "doc": "One measurement of one node over the run, as `(forward, value)` pairs.\n\nWhat a curve is drawn from — a gradient norm falling away, an update ratio\ndrifting. A fetch per `forward`, so `last=` is worth using."
        },
        {
          "name": "overlaid",
          "signature": "(graph: Graph, store: Store, *, run: str, thresholds: Thresholds | None = None, last: int | None = None, inside: Inside | None = None) -> Figure",
          "doc": "The graph, with what is wrong marked on the nodes it is wrong in — the\nanswer to *where*, which is the question a diagnosis of a distributed graph\nraises.\n\nHealth gets a **channel of its own**: the fill goes on saying where a node\nruns, the outline turns to the alarm colour, and the flags are a badge.\nRecolouring the fill would let *is this unhealthy* eat *where does this run*.\n\n`inside` is what `somatize.torch.architecture` gives back, and passing it is\nwhat makes a finding land **on the layer it is about**. Findings from a node\nwhose architecture was not drawn land on the node."
        },
        {
          "name": "profile",
          "signature": "(store: Store, *, run: str, of: str = 'grad_norm', thresholds: Thresholds | None = None, last: int | None = None) -> Figure",
          "doc": "One measurement per node, across the graph — the shape over depth. `of` is\nany number the audit wrote down. A node that raised a flag is drawn in the\nalarm colour and says which on its hover. Nodes are in declaration order,\nwhich for a chain is the order the gradient travelled, backwards."
        },
        {
          "name": "seen",
          "signature": "(store: Store, *, run: str, last: int | None = None) -> dict[str, dict[str, float | bool]]",
          "doc": "The numbers a verdict would be taken over — the latest of each.\n\nKeyed by node, and by `node.path.to.submodule` for anything `inside=` was\nasked to look at. The dot is what lets a figure colour the **node** while the\ndetail says which layer of it: *this node is unhealthy* is not an answer when\nthe node is twenty layers deep.\n\nFor looking at what was measured rather than at what somebody thinks of it,\nand for taking the verdict yourself with `verdict(seen[where], bounds)`."
        },
        {
          "name": "where",
          "signature": "(store: Store, *, run: str, thresholds: Thresholds | None = None, last: int | None = None, inside: Inside | None = None) -> Overlay",
          "doc": "A diagnosis folded onto the nodes of a graph: `{node: [flag, ...]}`.\n\nWhat `overlaid` hands to the figure, and what to hand to `graph.figure()`\nyourself if you are composing something else. A finding inside a node is\nnamed on its flag — `LEAKAGE in net.2` — because the box it lands on is the\nnode."
        },
        {
          "name": "within",
          "signature": "(where: str) -> str",
          "doc": "The node a key belongs to, whether or not it names a submodule of it."
        }
      ],
      "constants": []
    },
    {
      "name": "somatize.reasoning",
      "doc": "What somebody was trying to find out, read back and drawn.\n\n`somatize.record` is what happened; this is what was thought about it. Neither\nrecovers the other: **if it can be recalculated it is record, and if somebody\nthought it it is reasoning.**\n\nIt is written from the terminal, one move at a time, while somebody is\nthinking — `somatize-tree ask`, `suppose`, `tried`, `found`, `decide` — and read\nfrom here::\n\n    from somatize import Store\n    from somatize import reasoning\n\n    store = Store(\"/scratch/inv\")\n\n    reasoning.moves(store, tree=\"inv\")                  # every move, in the order they were made\n    reasoning.standing(store, tree=\"inv\")               # how each question and hypothesis stands\n    reasoning.says(store, tree=\"inv\")                   # what was said, and where it holds\n    reasoning.folds(store, tree=\"inv\")                  # what was abandoned, how many and why\n    reasoning.cites(store, tree=\"inv\")                  # which moves cite each commit\n    reasoning.covered(store, tree=\"inv\", by=[\"short\"])  # what a scope reaches\n    reasoning.figure(store, tree=\"inv\")                 # the shape of it\n\nEverything cross-references by **name**, because the store's slot stops\nidentifying a move the moment nobody is holding it in a variable — and reading\none back is exactly that moment.\n\n`tree=` and never `soma-tree.toml`: a second reader of that file is how a\n`--tree` said on the command line reaches the journal and not the walk, with\nnothing saying so.",
      "classes": [
        {
          "name": "Card",
          "signature": "(name: str, kind: str, x: float, y: float, w: float, h: float, said: str, prose: str, pruned: bool, hides: int | None = None, why: str | None = None)",
          "doc": "One move, placed. `hides` is how many a folded line takes with it, and is\n`None` on a card that is not folded — pruning says how many and why.",
          "exception": false,
          "constructors": [],
          "methods": [],
          "properties": []
        }
      ],
      "functions": [
        {
          "name": "cards",
          "signature": "(moves: Sequence[Row], says: Sequence[Row] = (), folds: Sequence[Row] = (), *, under: str | None = None) -> tuple[list[Card], list[Edge]]",
          "doc": "Where every card goes, and the edges between them.\n\nPure, over the rows `somatize.reasoning` reads — nothing here touches a\nstore, which is what makes the layout rules testable on their own.\n\n**Folding is what you hand it**: the lines in `folds` come folded, saying how\nmany they hide and why, and passing none opens everything. An app that folds\nwhat its reader has already read hands its own list and needs nothing from\nhere."
        },
        {
          "name": "cites",
          "signature": "(store: Store, *, tree: str) -> dict[str, dict[str, list[str]]]",
          "doc": "The way back: `{what: {id: [move, …]}}` — which moves cite each commit,\ntrial or configuration.\n\nA commit you cannot ask *what was this for* is a change without a motive.\nDerived from the citations here and **kept in no index**, so it is true the\nmoment somebody cites one and cannot go stale. On real data one commit came\nback cited by five attempts, told apart only by their configurations."
        },
        {
          "name": "covered",
          "signature": "(store: Store, *, tree: str, by: list[str]) -> list[str]",
          "doc": "What a scope with those roots reaches: the roots and everything under\nthem, in the order they were made.\n\nThe one walk a reader cannot redo by hand and get right, because `under` is\nmultivalued and a scope is a DAG and not a subtree. With it, *do these two\nscopes touch* is `set(a) & set(b)`. Fails if a name is nobody's here."
        },
        {
          "name": "figure",
          "signature": "(store: Store, *, tree: str, folded: bool = True, under: str | None = None) -> Figure",
          "doc": "The reasoning as a `plotly.graph_objects.Figure`, drawn from what is\nstored with nothing run again.\n\nHue says which of the five kinds a move is and never whether it went well;\nhow a question stands is written on it. The one outline that changes is\n`disputed` — edges of opposite sign whose scopes touch — because that is the\none a reader has to go and settle."
        },
        {
          "name": "folds",
          "signature": "(store: Store, *, tree: str) -> list[Row]",
          "doc": "The lines somebody abandoned: `root`, `by`, `course`, `why` and `hides`.\n\nOne row per move a decision named, with `hides` naming what folds with it —\nso how many is `len` and why is in words. **Pruning never deletes**: a line\nthat did not work is the most reusable thing an investigation produces.\n\nFolding what you have *read* is not here and never will be. It writes\nnothing down, because closing what you have read is not a claim about the\ninvestigation — it is the reader's, and so it is an app's."
        },
        {
          "name": "moves",
          "signature": "(store: Store, *, tree: str) -> list[Row]",
          "doc": "Every move, in the order they were made — which is the order siblings are\nread and drawn in.\n\n| column | what it is |\n|---|---|\n| `name` | what its author called it, and how it is reached everywhere else |\n| `id` | the store's slot; what says which of two variants was tried first |\n| `kind` | one of `KINDS` |\n| `prose` | what somebody wrote |\n| `under` | what it hangs under, multivalued: one move can answer two questions |\n| `about` | where it belongs when it hangs nowhere — a decision's scope names what it abandons |\n| `scope` | where it holds, as roots; empty is everywhere |\n| `cites` | `[{\"what\": …, \"id\": …}]`; only an attempt and a finding carry one |\n| `course` | `pursue`, `abandon`, `superseded`; only a decision |\n| `standing` | one of `STANDINGS`; only a question or a hypothesis, `None` otherwise |\n| `pruned` | whether something abandoned the line it is on — derived, never stored |\n| `who`, `when` | who wrote it down and at what second |\n\n`standing` is `None` and not `\"open\"` on an attempt: an attempt is not a\nquestion nobody has answered."
        },
        {
          "name": "says",
          "signature": "(store: Store, *, tree: str) -> list[Row]",
          "doc": "Everything anybody said from one move towards another: `from`, `says`,\n`to`, `scope`, `partly` and `withdrawn`, all in names.\n\n`answers`, `validates` and `refutes` are what make a standing; `combines`\nsays an attempt **is** the composition of those, which is what lets *each\nworked alone, together they cancel* read as what it is.\n\n`withdrawn` is an edge whose evidence came from a commit somebody judged\n`invalid`: it no longer counts towards a standing and is still written down,\nwhich is how a standing that moved on its own says what moved it."
        },
        {
          "name": "standing",
          "signature": "(store: Store, *, tree: str) -> dict[str, str]",
          "doc": "How each question and hypothesis stands, by name.\n\nA word, and the reason is in `says`: two edges of opposite sign whose scopes\n**touch** are `disputed`, and the same two that do not touch are `depends` —\nthe answer depending on the case, which is the most informative outcome an\ninvestigation gives. Use `covered` to tell one from the other.\n\nDerived every time and never read from a field, which is what lets one go\nback to `open` on its own: judge the commit a refutation rested on `invalid`\nand the edge stops counting, with nothing said again and nothing deleted."
        }
      ],
      "constants": [
        {
          "name": "KINDS",
          "type": "tuple",
          "repr": "('question', 'hypothesis', 'attempt', 'finding', 'decision')",
          "items": [
            "question",
            "hypothesis",
            "attempt",
            "finding",
            "decision"
          ]
        },
        {
          "name": "STANDINGS",
          "type": "tuple",
          "repr": "('open', 'answered', 'partly', 'validated', 'partly-validated', 'refuted', 'partly-refuted', 'disputed', 'depends')",
          "items": [
            "open",
            "answered",
            "partly",
            "validated",
            "partly-validated",
            "refuted",
            "partly-refuted",
            "disputed",
            "depends"
          ]
        }
      ]
    },
    {
      "name": "somatize.record",
      "doc": "What happened, read back.\n\nA `Recorder` writes one record per `forward`; this is how it is read::\n\n    from somatize import Recorder, Store\n    from somatize.record import curve, forwards, nodes, runs\n\n    store = Store(\"/scratch/runs\")\n    Trainer(g, objective=..., optimizer=...,\n            watching=Recorder(store, run=\"tuesday\", summarising=[\"loss\"]))\n\n    runs(store)                            # what is in here at all\n    forwards(store, run=\"tuesday\")         # step by step, one scan\n    curve(store, run=\"tuesday\")            # the losses, one scan\n    nodes(store, run=\"tuesday\", last=50)   # who spent the time, a fetch each\n\n**Functions and not a type**, like `gather` and `take`: what is being read is a\nfolder, and a class around a store would only be the store with a longer name.\n\nThere are two ways to see a run and they are not rivals. While it is going, what\nyou want arrives at `watching=` and costs nothing; when it is over — or when it\nis **another machine's** — there is no connection and a scan is all there is.\nBoth answer in the same shape, so whatever draws one draws the other.",
      "classes": [
        {
          "name": "Live",
          "signature": "(*, title: str = 'live', smooth: int | None = None, every: int = 1)",
          "doc": "A run drawn while it happens. Hand it to `watching=`::\n\n    live = Live()\n    live                                   # the cell shows it\n    g.forward(x, watching=live)\n\nWith `ipywidgets` it redraws in place; without one it still tallies\neverything and `figure()` gives the same picture. It holds one row of numbers\nper `forward`, so watching for an afternoon does not grow with it, and keeps\nnothing on disk — a `Live` beside a `Recorder` is the normal pairing.",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "figure",
              "signature": "() -> Figure",
              "doc": "The same figure `progress` draws, from what has arrived so far."
            },
            {
              "name": "widget",
              "signature": "() -> Any",
              "doc": "A `FigureWidget` that redraws in place, or `None` without ipywidgets.\n\nAsked for by hand rather than made on construction: building one imports\nipywidgets, and a `Live` used from a script has no business needing it."
            }
          ],
          "properties": []
        }
      ],
      "functions": [
        {
          "name": "curve",
          "signature": "(store: Store, *, run: str, of: str = 'loss.value') -> list[tuple[int, float]]",
          "doc": "One number per `forward`, as `(forward, value)` pairs — a training curve.\n`of` names the field: `loss.value` when the recorder summarised `loss`, and\notherwise anything a fact carries. Free when the field is in the record and\none fetch per `forward` when it is not, which `curve_costs` says out loud."
        },
        {
          "name": "curve_costs",
          "signature": "(store: Store, *, run: str, of: str = 'loss.value') -> str",
          "doc": "Whether `curve` would scan or fetch: `\"scan\"` or `\"fetch\"`.\n\nAsked before drawing something ten thousand steps long, and the answer is\n`\"fetch\"` when the recorder was not told to summarise that kind."
        },
        {
          "name": "facts",
          "signature": "(store: Store, *, run: str, forward: int) -> list[Fact] | None",
          "doc": "Everything that happened in one `forward`, in the order it arrived. The\ndetail, and the one call that costs a fetch. Each fact is the same dict a\n`watching=` callable was handed, so what you looked at live is what you read\nback. `None` for no such `forward`, which is not a failure."
        },
        {
          "name": "fleet",
          "signature": "(store: Store, *, run: str, last: int | None = None) -> list[Row]",
          "doc": "What each machine did, as the inverse of `nodes`. The record is written\nrun → `forward` → node with *where* as an attribute, and this turns it the\nother way up.\n\n**There is no registry and no heartbeat.** A machine is here because it did\nsomething, and what it did is already written down.\n\n`busy`, `memory`, `cores`, `up_us` and `served` are the half **no record can\nderive** — the worker says them itself, down the connection that is already\nopen. The newest reading and not an average; `None` is a machine that did not\nsay.\n\n`waiting_us` is the column that only exists up here: the round trip **minus**\nwhat actually ran over there — the wire, the queue and the codec — and\nneither half of that subtraction belongs to a node.\n\nA scan and a fetch per `forward`, the same as `nodes`; `last=N` reads only\nthe last N."
        },
        {
          "name": "gantt",
          "signature": "(store: Store, *, run: str, forward: int = 0) -> Figure",
          "doc": "One `forward` on a timeline: what ran when, and what was waiting.\n\nThe picture `spent` cannot draw. A total says a node cost four hundred\nmilliseconds; this says whether those were **beside** the rest of the graph\nor in front of it.\n\nEvery fact carries `began_us`, so a `Wave` shows as overlapping bars. A slice\nthat ran elsewhere counts from **its own** start and is shifted by the `left`\nit arrived under — two wall clocks would not have composed. One `forward` and\nnot an average, because an average of timelines is not one."
        },
        {
          "name": "machines",
          "signature": "(store: Store, *, run: str, last: int | None = None) -> Figure",
          "doc": "The run, per machine: what each one worked and what it was waited on.\n\nThe inverse of `spent`, and the split is the whole of it: a bar is the round\ntrip in two parts — time spent **working**, and time **waited on**, which is\nthe wire, the queue and the codec. Neither half belongs to a node, which is\nwhy no per-node view can draw this. Costs what `fleet` costs."
        },
        {
          "name": "forwards",
          "signature": "(store: Store, *, run: str) -> list[Row]",
          "doc": "Every `forward` of that run, in order, as the record says it. One scan and\nno fetches, which is what makes this what a progress view reads in a loop.\nAnything the recorder was told to summarise comes back under its own\n`<kind>.<field>` name, as text."
        },
        {
          "name": "nodes",
          "signature": "(store: Store, *, run: str, last: int | None = None) -> list[Row]",
          "doc": "What each node did over the run, added up: how many times it ran, how long\nin total and on average, how often it was read back instead, and where. **It\ncosts a fetch per `forward`**, because which node did what is in the blobs;\n`last=N` reads only the last N."
        },
        {
          "name": "progress",
          "signature": "(store: Store, *, run: str, smooth: int | None = None) -> Figure",
          "doc": "How a run went, step by step: the loss, the time, and what broke. One scan\nwhen the recorder summarised the loss and a fetch per `forward` when it did\nnot — `curve_costs` says which. `smooth` is the window of the rolling mean,\nin `forward`s; `0` draws only what was measured."
        },
        {
          "name": "runs",
          "signature": "(store: Store) -> list[Row]",
          "doc": "Every run this store holds, newest last. One scan and no fetches: how many\n`forward`s each has, how many broke, when the first and last were written,\nand how long they took. The one to call first, because a store holds whatever\nanybody put in it."
        },
        {
          "name": "spent",
          "signature": "(store: Store, *, run: str, last: int | None = None) -> Figure",
          "doc": "Where the time went, added up per node. Bars are coloured by **where** the\nnode ran, from the same table the graph is drawn with. A fetch per `forward`,\nbecause which node did what is in the blobs; `last=N` reads only the last N."
        },
        {
          "name": "standing",
          "signature": "(store: Store) -> dict[str, Row]",
          "doc": "Every machine writing readings into this store, as `{id: reading}`.\n\nThe **idle** half: a worker says what it looks like on a clock whether or not\nanybody is asking it for anything, and it goes here rather than down a wire\nbecause a client only reads the socket while a job is in flight.\n\nKeyed by what the machine calls **itself**, since on this path there is no\nclient and `w1` is the client's word; `fleet` joins the two. `quiet_s` is how\nfar behind the newest reading each one is, measured **writer against writer**\nand never against this machine's clock. One scan and no fetches."
        }
      ],
      "constants": []
    },
    {
      "name": "somatize.study",
      "doc": "Level 3: what is above one training run.\n\nThe graph is a network — one ``forward``. The ``Trainer`` is a training run — an\nafternoon. This is the level above and, like a federated round, it has **no\ntype**: N training runs are a ``for``::\n\n    from somatize.study import Partition, Pruner, Sampler, Space\n    from somatize.torch import Trainer\n\n    space = Space().real(\"lr\", 1e-5, 1e-1, log=True).choice(\"opt\", [\"adam\", \"sgd\"])\n    sampler, finished = Sampler.tpe(goal=\"min\"), []\n\n    for trial in range(50):\n        point = sampler.ask(space, trial, finished)\n        g = build(**point)                       # a Point is a mapping\n        t = Trainer(g, objective=cross_entropy, optimizer=Adam(parameters(g)))\n        finished.append((point, t.fit(data, epochs=10).loss))\n\nWhat lives here are the pieces that ``for`` asks for, all of one shape: **numbers\nin, a decision out — never a tensor**. That is what lets it be Rust while the\nloop stays in Python.\n\nA ``Pruner`` says whether a trial going badly is worth another epoch, and it\n**stops nothing** — it answers and the loop stops calling::\n\n    for epoch in range(50):\n        reported.append(t.fit(data, epochs=1).loss)\n        if why := pruner.verdict(reported, finished):\n            break\n\nThe three schemes differ in what they judge against: ``median``/``percentile``\nthe other trials, ``threshold`` a constant, ``patience`` the trial itself. The\nsamplers differ in what they **look at**: ``grid`` at the space's shape and it is\nthe one that runs out, ``random``/``halton``/``sobol`` at nothing, ``tpe`` at what\nalready happened. All but ``tpe`` derive their point from the seed and the\n**index**, so a machine that claimed trial 7 gets the same point without\nreplaying six.\n\n``Partition`` is five schemes and not sklearn's fifteen, because stratifying is a\nk-fold inside each class and grouping a k-fold over the groups; the rest are\nparameters. It is not called ``Split``: ``somatize.torch.Split`` is already split\nlearning.",
      "classes": [
        {
          "name": "Partition",
          "signature": null,
          "doc": "`somatize.study.Partition` — how the samples are cut into folds.",
          "exception": false,
          "constructors": [
            {
              "name": "grouped",
              "signature": "(k)",
              "doc": "`k` folds where all the samples of a group land on the same side. Needs\n`groups=`, and takes no seed: it places the biggest groups first, which\nis what keeps the folds comparable."
            },
            {
              "name": "kfold",
              "signature": "(k, *, shuffle=None)",
              "doc": "`k` folds over the samples, each held out in turn. `shuffle` is the\nseed, and without one the order they came in is kept."
            },
            {
              "name": "stratified",
              "signature": "(k, *, shuffle=None)",
              "doc": "`k` folds where every class keeps the share it has in the whole. Needs\n`classes=`."
            },
            {
              "name": "stratified_grouped",
              "signature": "(k)",
              "doc": "Groups whole, and among the ways of doing that the one leaving the\nclasses most even. Needs both."
            },
            {
              "name": "time_series",
              "signature": "(k, *, gap=0)",
              "doc": "`k` growing prefixes, so nothing is ever trained on its own future.\n`gap` drops that many samples between the two sides."
            }
          ],
          "methods": [
            {
              "name": "folds",
              "signature": "(n, *, classes=None, groups=None)",
              "doc": "The folds as `(train, test)` pairs of indices — sklearn's shape, so a\nloop written against `KFold().split()` reads the same.\n\n`classes=` and `groups=` are one small integer per sample. They are\n**numbers, not labels**: turn `y` into them where `y` already is, with\n`.tolist()` or a dictionary, and no tensor crosses."
            }
          ],
          "properties": [
            {
              "name": "k",
              "doc": "How many folds it produces, without producing them."
            }
          ]
        },
        {
          "name": "Point",
          "signature": null,
          "doc": "`somatize.study.Point` — one configuration.\n\nIt behaves as a mapping, so `build(**point)` and `point[\"lr\"]` both work, and\n`str(point)` is the trial's **name** — derived from the values in the space's\norder, so two machines that never spoke file it identically.",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "items",
              "signature": "()",
              "doc": "Both, paired."
            },
            {
              "name": "keys",
              "signature": "()",
              "doc": "The knobs, in the space's order. What makes `**point` work."
            },
            {
              "name": "values",
              "signature": "()",
              "doc": "The values, in the same order."
            }
          ],
          "properties": []
        },
        {
          "name": "Pruner",
          "signature": null,
          "doc": "`somatize.study.Pruner` — whether a trial that is going badly is worth\nanother epoch.",
          "exception": false,
          "constructors": [
            {
              "name": "diverged",
              "signature": "()",
              "doc": "Only what blew up: no bounds, so nothing goes but a loss that is not a\nnumber."
            },
            {
              "name": "median",
              "signature": "(*, goal='min', warmup=0, startup=1)",
              "doc": "Prune what is behind the median of the trials that already finished.\n`Pruner.percentile(50, …)` with a name on it."
            },
            {
              "name": "patience",
              "signature": "(steps, *, min_delta=0.0, goal='min')",
              "doc": "Prune what has stopped improving on its own best — early stopping.\n`steps` cannot be zero."
            },
            {
              "name": "percentile",
              "signature": "(p, *, goal='min', warmup=0, startup=1)",
              "doc": "The same with the share that survives said out loud: **smaller prunes\nmore**. At `50` the better half stays."
            },
            {
              "name": "threshold",
              "signature": "(*, lower=None, upper=None)",
              "doc": "Prune what leaves bounds you already know are hopeless. The only scheme\nthat needs no other trial, so it works on the very first."
            }
          ],
          "methods": [
            {
              "name": "verdict",
              "signature": "(mine, others=None)",
              "doc": "Why this trial is not worth another epoch, or `None` to carry on.\n\n`mine` is what it has reported so far, in order; `others` the same for\nthe trials that already finished. A \"step\" is the **n-th report**, so\ntrials have to report on the same schedule for the comparison across\nthem to mean anything.\n\nNothing is stopped here. You stop calling the trainer::\n\n    if why := pruner.verdict(reported, finished):\n        break"
            }
          ],
          "properties": []
        },
        {
          "name": "Sampler",
          "signature": null,
          "doc": "`somatize.study.Sampler` — where to look for the next configuration.",
          "exception": false,
          "constructors": [
            {
              "name": "grid",
              "signature": "(steps=5)",
              "doc": "Every combination, then nothing. `steps` says how finely a continuous\nknob is cut; an `int` narrower than that is taken whole."
            },
            {
              "name": "halton",
              "signature": "(*, seed=0)",
              "doc": "Cover the space evenly instead of drawing from it evenly, one prime per\nknob.\n\nA uniform draw is even *in expectation*: nothing stops the next two\ntrials from landing on top of each other, it is only unlikely. This is\neven *for every prefix*, which is what a study handed out of a shared\nfolder wants — two machines taking different numbers do not collide, and\nnot because collision is improbable.\n\nIts cover thins once there are many knobs, and it has no ceiling."
            },
            {
              "name": "random",
              "signature": "(*, seed=0)",
              "doc": "Uniform in every knob, looking at nothing else — and over a space where\nonly a few knobs matter, that beats a grid on the same budget."
            },
            {
              "name": "sobol",
              "signature": "(*, seed=0)",
              "doc": "The same, without the seam — and with a ceiling of 32 knobs.\n\nEvery knob is read in base two and told apart by a table of direction\nnumbers (Joe and Kuo, 2008), so nothing thins out as the knobs grow. Past\nwhat the table reaches, `ask` answers `None` from the very first trial."
            },
            {
              "name": "tpe",
              "signature": "(*, goal='min', startup=10, candidates=24, quantile=0.25, seed=0)",
              "doc": "Guided by what already worked: model the good trials, model the bad ones,\nand propose where the first is likely and the second is not.\n\nRandom until `startup` trials have finished. Unlike the other two its\nanswer depends on what the asking machine has seen, which is what being\nguided means."
            }
          ],
          "methods": [
            {
              "name": "ask",
              "signature": "(space, trial, seen=None)",
              "doc": "Where to look for the `trial`-th time, or `None` when there is nowhere\nleft — which is a grid saying it is done, and how a `for` stops without\nbeing told a number.\n\n`seen` is `(point, score)` for the places somebody has already been.\n**A score of `None` means the trial is still running** — another machine\nis trying it and nobody knows yet how it will do, which is what\n`in_flight` gives back. Four of the five schemes ignore the whole\nargument, and that is the point of having five."
            },
            {
              "name": "total",
              "signature": "(space)",
              "doc": "How many combinations there are, for the one scheme that has an answer —\n`None` for the two that never run out. What a `range()` wants."
            }
          ],
          "properties": []
        },
        {
          "name": "Space",
          "signature": "()",
          "doc": "`somatize.study.Space` — what is being searched over.\n\nBuilt up, and every call gives back a **new** space: the one you had is still\nthe one you had, which is what makes handing the same base to two studies\nsafe.",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "choice",
              "signature": "(name, options)",
              "doc": "A knob that is one of these, by name."
            },
            {
              "name": "int",
              "signature": "(name, low, high)",
              "doc": "A knob that is a whole number between the two, both included."
            },
            {
              "name": "names",
              "signature": "()",
              "doc": "The knobs, in declaration order."
            },
            {
              "name": "read",
              "signature": "(said)",
              "doc": "The point that text names, read against these knobs.\n\nThe other half of `str(point)`, and it needs the space in front of it:\n`batch=64` on its own does not say whether 64 is a whole number or an\noption spelt `\"64\"`.\n\nIt is what makes a study's history come back in **one scan** of the\nshared folder: a trial keeps its configuration as text next to its score,\nso nothing has to be fetched to know where it looked."
            },
            {
              "name": "real",
              "signature": "(name, low, high, *, log=False)",
              "doc": "A knob that is anything between the two.\n\n`log=True` draws it evenly in the **logarithm**, which is the only sane\nway to search a learning rate: drawn linearly, four fifths of\n`1e-5..1e-1` sits above `0.02`."
            }
          ],
          "properties": []
        }
      ],
      "functions": [
        {
          "name": "abandoned",
          "signature": "(store: Store, *, study: str, stale: float = 3600.0) -> list[tuple[int, int]]",
          "doc": "Which trials have stopped moving, as `(trial, attempt)` pairs.\n\nIt **decides nothing**: whether a quiet trial is dead, preempted or on a very\nlong epoch is not something a folder can tell. So this reports and the loop\nchooses::\n\n    for trial, attempt in abandoned(store, study=STUDY):\n        take(store, point, study=STUDY, trial=trial, me=me, attempt=attempt + 1)\n\nTaking the next attempt rather than writing over the old record, for the same\nreason `claim` uses a link. Being wrong is cheap: too eager is a trial run\ntwice, and a claim still cannot collide."
        },
        {
          "name": "coordinates",
          "signature": "(store: Store, space: Space, *, study: str, goal: str | None = None) -> Figure",
          "doc": "Every finished trial as a **curve** across the knobs, coloured by score.\nThe one picture that shows a *region* of the space rather than one knob at a\ntime.\n\nDrawn by hand out of splines rather than with plotly's `Parcoords`, which\nonly draws straight segments. That costs its brushing and buys a trial\nreading as one continuous thing. A curve is an interpolation **between axes,\nwhere there is nothing to be wrong about** — a point exists only where it\ncrosses an axis — and still drawn gently, because a curve bulging past the\ntop of an axis reads as a value beyond its range.\n\nNo colour scale beside it: the score is the last axis. Unlike `table` there\nis no fallback, so a study that recorded no direction and a caller that names\nnone is an error rather than a guess."
        },
        {
          "name": "curves",
          "signature": "(store: Store, *, study: str) -> list[list[float]]",
          "doc": "The reports of every trial that ran to the end — what a `Pruner` wants.\nThe reader that pays: a curve grows, so it lives in the blob and this is a\nscan **plus one fetch per trial**."
        },
        {
          "name": "direction",
          "signature": "(store: Store, *, study: str) -> str | None",
          "doc": "Which way is better in this study — `\"min\"`, `\"max\"`, or `None`. One scan\nand no fetches.\n\n`None` means no trial said, and it is the honest answer rather than `\"min\"`.\nWhen records disagree the newest wins — the direction is what the person\nrunning the study currently means. Ties break by the **higher trial number**,\nbecause a study writes its first records inside the same second."
        },
        {
          "name": "finished",
          "signature": "(store: Store, space: Space, *, study: str) -> list[tuple[Point, float]]",
          "doc": "Every trial that ran to the end, as `(point, score)` — what `ask` wants,\nin **one scan and no fetches**.\n\nPruned trials are left out on purpose: a pruned score is real but was\nmeasured after fewer epochs, so a sampler that treats it as a bad\nconfiguration learns something untrue."
        },
        {
          "name": "importance",
          "signature": "(store: Store, space: Space, *, study: str) -> list[tuple[str, float]]",
          "doc": "How decisive each knob was, as `(name, |rho|)`, biggest first.\n\n**Spearman's rho**: how well the score follows a knob monotonically, without\nassuming a shape. Ranks rather than values, so a knob searched in log needs\nno special case, and only the trials that ran to the end.\n\nA categorical knob is ranked by its own options in order, which is honest for\ntwo and thin beyond that — answered anyway, because leaving it out would be\nthis deciding what you may look at. `0.0` where a knob never varied: no\nevidence, which is not no effect."
        },
        {
          "name": "in_flight",
          "signature": "(store: Store, space: Space, *, study: str, stale: float = 3600.0) -> list[tuple[Point, float | None]]",
          "doc": "The trials another machine is holding, **each with no score**.\n\nHand these to a sampler beside `finished` and a guided one stops proposing\nnext to what somebody else is already trying::\n\n    point = sampler.ask(space, trial,\n                        finished(store, space, study=STUDY)\n                        + in_flight(store, space, study=STUDY))\n\nThat is *constant liar* (Ginsbourger, Le Riche and Carraro, 2010) without the\nlie, and the difference was measured. Handing the sampler a made-up bad score\n**backfires**: `Tpe` sizes the pile it imitates as a share of everything it is\nhanded, so one more point raises the quota and can promote a trial out of the\nbad pile — one proposal in two hundred landed on the occupied region without\nit, thirty-nine with it. So `None` says *running* and does not vote.\n\nOne scan and **no fetches**. `stale` is how far behind a `running` trial may\nfall before it counts as stopped, measured **against the newest write in this\nstudy and not against this machine's clock** — two machines sharing a folder\nare two clocks that disagree by minutes."
        },
        {
          "name": "influence",
          "signature": "(store: Store, space: Space, *, study: str) -> Figure",
          "doc": "How decisive each knob was: |rho| against the score, biggest first.\n\nA rank correlation, so it says *this knob orders the results* and not *this\nknob is worth these many points*."
        },
        {
          "name": "report",
          "signature": "(store: Store, point: Point, reports: Sequence[float], *, study: str, trial: int, me: object, attempt: int = 0, state: str = 'running', score: float | None = None, because: str | None = None, took: float | None = None, goal: str | None = None) -> None",
          "doc": "Writes down where this trial has got to, as often as there is something to\nsay — once an epoch makes a curve watchable from another machine while it is\nstill being drawn.\n\nOnly the machine that claimed it writes, and nothing has to enforce that:\nnobody else could have got the claim. `goal` goes beside the score because a\nscore without it is not readable by anybody without this script."
        },
        {
          "name": "table",
          "signature": "(store: Store, space: Space, *, study: str, goal: str | None = None) -> Figure",
          "doc": "Every scored trial, best first, with the configuration that got it. One\nscan and no fetches. Pruned trials are here too and say so.\n\n*Best* needs a direction, which comes from the record; `goal` overrides it.\nWhen neither says, the trials come back **in the order they were run** and\nthe title says so — a table headed *best first* sorted the wrong way round is\nworse than one that is not sorted."
        },
        {
          "name": "take",
          "signature": "(store: Store, point: Point, *, study: str, trial: int, me: object, attempt: int = 0, goal: str | None = None) -> bool",
          "doc": "Claims the `trial`-th trial of `study`. `True` when it is this machine's;\n`False` means somebody else got there first and the loop goes on to the next\nnumber. `goal` is written here as well as on every `report`, so a trial\nclaimed and never reported still says which way it was looking."
        },
        {
          "name": "trials",
          "signature": "(store: Store, space: Space, *, study: str) -> list[Trial]",
          "doc": "Every trial of this study, whatever state it is in, as records. The one for\nlooking rather than deciding: what is still running, and whether the study is\ndone."
        }
      ],
      "constants": [
        {
          "name": "DONE",
          "type": "str",
          "repr": "'done'"
        },
        {
          "name": "FAILED",
          "type": "str",
          "repr": "'failed'"
        },
        {
          "name": "MAX",
          "type": "str",
          "repr": "'max'"
        },
        {
          "name": "MIN",
          "type": "str",
          "repr": "'min'"
        },
        {
          "name": "PRUNED",
          "type": "str",
          "repr": "'pruned'"
        },
        {
          "name": "RUNNING",
          "type": "str",
          "repr": "'running'"
        },
        {
          "name": "STALE",
          "type": "float",
          "repr": "3600.0"
        }
      ]
    },
    {
      "name": "somatize.torch",
      "doc": "What only makes sense with torch in front of you: training.\n\nThe core does not know what a loss is, nor a gradient, nor an optimizer, and it\nis not going to — writing this neutrally would ask for a `Backend` with a single\nimplementor. So it lives here, in Python, and `core/` does not change a line::\n\n    from somatize import Graph\n    from somatize.torch import Trainer, parameters\n\n    g = Graph.somatize(Encoder().on(\"cuda:0\") >> Head().on(\"cuda:0\"))\n    t = Trainer(g, objective=cross_entropy,\n                optimizer=torch.optim.Adam(parameters(g), lr=1e-3))\n    t.fit(data, epochs=10)\n\n`Split` — and whatever else is written into the `Learning` hole — is for the half\nthat cannot be trained from here. A node on another machine gets no gradient from\na `backward()` run in this process, so a **trainer travels to it**: it keeps the\nactivation where its autograd graph is, is handed `dL/d(what the node produced)`\nand carries on under an optimizer of its own. The node is not asked to know any\nof it. Split learning, greedy, forward-forward and synthetic gradients are the\nsame hole answered four ways::\n\n    Trainer(g, objective=cross_entropy, optimizer=Adam(parameters(g), lr=1e-3),\n            trains={\"body\": Split(SGD, lr=0.1)}, broker=broker)\n\nImporting this also says how a tensor is written down, which is what lets a\ngraph keep what it produces: see `somatize.torch._codec`.\n\nTraining does not touch the graph: afterwards its nodes, its edges, its plan and\nits placement are the same. What changes are the weights, which live inside the\nnodes and always did.\n\nThat the package is called `torch` does not shadow the real one: in Python 3\nimports are absolute, so `import torch` in here brings the usual one.",
      "classes": [
        {
          "name": "Audit",
          "signature": "(*, every: int = 1, snapshot: int = 50, channels: bool = False, groups: Mapping[str, Mapping[str, Iterable[int]]] | None = None, window: int = 20, inside: Any = None, most: int = 32)",
          "doc": "Watches the nodes of a graph and says what it saw. Built by the `Trainer`\nwhen `auditing=` is given; little reason to make one by hand except to choose\na cadence or to declare `groups`, the channel partitions a node keeps apart::\n\n    Trainer(g, ..., auditing=Audit(every=10, channels=True))\n    Audit(groups={\"encoder\": {\"audio\": range(0, 64), \"text\": range(64, 128)}})",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "observed",
              "signature": "(graph: Graph) -> list[Fact]",
              "doc": "One `health` fact per node, for the step that just finished.\n\nCalled after `backward`, the only moment when this step's activations and\nthe gradients for them are both in hand. Empty on an unmeasured step."
            },
            {
              "name": "release",
              "signature": "() -> None",
              "doc": "Takes the hooks off. A hook nobody removed is a graph nobody can\ngarbage-collect, and a second `watch` would double every count."
            },
            {
              "name": "watch",
              "signature": "(graph: Graph) -> Audit",
              "doc": "Hooks every node of this graph, and — with `inside=` — what is in it.\n\nWhat comes back is keyed by node, and by `node.path.to.submodule` for\nanything inside one. The dot is what lets a figure colour the **node**\nwhile the hover says which layer of it."
            }
          ],
          "properties": []
        },
        {
          "name": "Learning",
          "signature": "(optimizer: Callable[..., Any], *, every: int | None = None, **how: Any)",
          "doc": "What trains one node, on the machine that node runs on.\n\nA technique writes `learn(signal, ctx)` and nothing else: handed\n`dL/d(what the node produced)`, it gives back `dL/d(what it was given)`, or\n`None`. It reaches the node through `held` — this step's activation — and\n`given` — the leaf its input became; both are dropped after each `learn`.",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "accumulating",
              "signature": "(every: int | None) -> Learning",
              "doc": "How many steps go into one update, unless this one already said. A\ntechnique that named its own **wins**, the same rule `trains` follows for\nwho trains whom."
            },
            {
              "name": "at",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "beside",
              "signature": "() -> tuple[Enters, Learning]",
              "doc": "Its two positions in a graph: the one that leafs the input, and\nitself, which keeps what the node produced."
            },
            {
              "name": "cached",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "closes",
              "signature": "() -> bool",
              "doc": "Whether this step ends one, which is where the optimizer moves."
            },
            {
              "name": "done",
              "signature": "() -> Any",
              "doc": "Lets go of this step's activation, and gives back what the node was\ngiven so a gradient can be read off it. Every `learn` ends here."
            },
            {
              "name": "entering",
              "signature": "(value: Any, ctx: Ctx) -> Any",
              "doc": "The input as a leaf, remembered, which is what makes `dL/d(input)` a\nthing that exists. Called from the other position."
            },
            {
              "name": "forward",
              "signature": "(value: Any, ctx: Ctx) -> Any",
              "doc": "As a node, in the position after the one it trains: an ordinary value\nis the activation to keep, and an envelope is the gradient to learn\nfrom."
            },
            {
              "name": "frozen",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "learn",
              "signature": "(signal: torch.Tensor | None, ctx: Ctx) -> Any",
              "doc": "What to do with `dL/d(what the node produced)`, and what to give back.\n\nThe hole. Whatever a technique is, it is this method — and `signal` being\n`None` is nobody owing it anything this step."
            },
            {
              "name": "mapped",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "named",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "of",
              "signature": "(node: Any) -> Learning",
              "doc": "The node it trains. Said when the graph is put together, because that\nis the only moment somebody has both."
            },
            {
              "name": "on",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "opens",
              "signature": "() -> bool",
              "doc": "Whether this step starts a group, which is where gradients are cleared\nrather than added to."
            },
            {
              "name": "training",
              "signature": "() -> list[torch.nn.Parameter]",
              "doc": "Which parameters it updates: the node's, and whatever else a technique\nbrought with it — a decoder, a guesser — which is why it is a method and\nnot a line."
            },
            {
              "name": "waiting",
              "signature": "() -> Any",
              "doc": "The activation this step left, or `OutOfStep` if there is none. Not a\n`None` walking into an optimizer: a gradient for an activation that is\nnot there means the two halves are a step apart."
            }
          ],
          "properties": [
            {
              "name": "optimizer",
              "doc": "Its optimizer, built the first time it is asked for — over the\nparameters of wherever it ended up, which is the whole reason it is built\nhere and not by whoever declared it."
            }
          ]
        },
        {
          "name": "NoGradient",
          "signature": null,
          "doc": "Something the optimizer holds never got a gradient. Its own type because\nit is worth catching: with a cut on purpose — split learning — this is what\nyou expect, and you say so by taking those parameters out of the optimizer.",
          "exception": true,
          "constructors": [],
          "methods": [],
          "properties": []
        },
        {
          "name": "OutOfStep",
          "signature": null,
          "doc": "A gradient arrived for an activation that is not there any more.\n\nThrough a `Graph` it comes back as the text of a `ValueError`: a node's\nfailure crosses as a message and not as a type.",
          "exception": true,
          "constructors": [],
          "methods": [],
          "properties": []
        },
        {
          "name": "Result",
          "signature": "(history: list[float])",
          "doc": "What a training run leaves behind: the loss, step by step.",
          "exception": false,
          "constructors": [],
          "methods": [],
          "properties": [
            {
              "name": "loss",
              "doc": "The last loss, or `None` if not a single step was taken."
            }
          ]
        },
        {
          "name": "Split",
          "signature": "(optimizer: Callable[..., Any], *, every: int | None = None, **how: Any)",
          "doc": "Split learning: carry on with the chain rule from the gradient of the\nseam, step, and hand back the gradient of the input. With a group of more\nthan one step the same three movements are taken apart — cleared where the\ngroup opens, added to in between, stepped where it closes.",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "accumulating",
              "inherited_from": "somatize.torch.Learning"
            },
            {
              "name": "at",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "beside",
              "inherited_from": "somatize.torch.Learning"
            },
            {
              "name": "cached",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "closes",
              "inherited_from": "somatize.torch.Learning"
            },
            {
              "name": "done",
              "inherited_from": "somatize.torch.Learning"
            },
            {
              "name": "entering",
              "inherited_from": "somatize.torch.Learning"
            },
            {
              "name": "forward",
              "inherited_from": "somatize.torch.Learning"
            },
            {
              "name": "frozen",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "learn",
              "signature": "(signal: torch.Tensor | None, ctx: Ctx) -> Any",
              "doc": "What to do with `dL/d(what the node produced)`, and what to give back.\n\nThe hole. Whatever a technique is, it is this method — and `signal` being\n`None` is nobody owing it anything this step."
            },
            {
              "name": "mapped",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "named",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "of",
              "inherited_from": "somatize.torch.Learning"
            },
            {
              "name": "on",
              "inherited_from": "somatize.Node"
            },
            {
              "name": "opens",
              "inherited_from": "somatize.torch.Learning"
            },
            {
              "name": "training",
              "inherited_from": "somatize.torch.Learning"
            },
            {
              "name": "waiting",
              "inherited_from": "somatize.torch.Learning"
            }
          ],
          "properties": [
            {
              "name": "optimizer",
              "inherited_from": "somatize.torch.Learning"
            }
          ]
        },
        {
          "name": "Trainer",
          "signature": "(graph: Graph, *, objective: Objective, optimizer: Any = None, trains: dict[str, Learning] | None = None, every: int = 1, micro: int = 1, store: Store | str | None = None, broker: Broker | None = None, watching: Any = None, auditing: Any = None)",
          "doc": "Trains a graph, without the graph finding out — no `g.fit(...)`, so the\nsame graph can be trained three ways without touching it::\n\n    t = Trainer(g, objective=cross_entropy,\n                optimizer=torch.optim.Adam(parameters(g), lr=1e-3))\n\nThe optimizer is the caller's, which keeps a name registry\n(`optimizer=\"adam\"`) out. `store` is a directory, and makes a settled\n`.cached()` prefix run once per batch. `broker` says who knows where each\nhost is, as in `Graph.forward`.\n\nTraining a graph with a slice on another machine is **not** training that\nslice: what crosses a wire is the value and not the graph that made it.\n`trains` is how that half gets trained anyway, said **here** because it is a\nfact of this training run and not of the graph::\n\n    Trainer(g, objective=cross_entropy,\n            optimizer=Adam(parameters(g), lr=1e-3),   # the half that is here\n            trains={\"body\": Split(SGD, lr=0.1)},      # the half that is not\n            broker=Broker.embedded({\"gpu\": Worker.at(\"node3:7000\")}))\n\nThat puts a trainer **beside** the node rather than inside it, so the node is\nnever asked to know it is being trained. Those weights are that trainer's, so\nthey come out of this optimizer — `parameters(g, without=trains)` — and\nholding both is refused rather than quietly updating them twice.\n\n`every` is how many steps go into one update and `micro` how many pieces one\nstep is cut into; they **multiply** rather than compete. `watching` is told\nwhat happens and is handed on to every `forward`, so one stream carries both\nthe engine's vocabulary and this level's `loss` and `updated`.",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "export",
              "signature": "() -> Weights",
              "doc": "What this training run learnt: its weights, node by node, as\n`{node_id: {key: tensor}}`, by the same two ducks everything here asks by.\n\nA **snapshot** and not a view: detached and copied, so the next step does\nnot move it under whoever is holding it. The optimizer's state is not in\nit — momentum is this client's. Refused for a node trained **and**\nrunning elsewhere: the copy here never learnt anything."
            },
            {
              "name": "fit",
              "signature": "(data: Iterable[Batch], epochs: int = 1) -> Result",
              "doc": "Takes one step per batch, for as many epochs as you say.\n\n`data` is walked once per epoch, so with more than one it has to be\nre-iterable: a generator is exhausted on the first."
            },
            {
              "name": "load",
              "signature": "(weights: Weights) -> None",
              "doc": "The mirror of `export`. Every node it names has to be here with the\nweights and shapes it says, and nothing is copied in until all of that is\ntrue, so a refusal leaves the net as it was rather than half loaded."
            },
            {
              "name": "step",
              "signature": "(batch: Batch) -> float",
              "doc": "One step: forward, loss, backward, and update when the group closes.\nReturns the loss **whole** — divided for the backward pass and not for\nwhoever is reading, or a history would change shape with `every`.\n\n**The primitive**, and `fit` is sugar on top: whatever does not fit in an\nepoch loop is written as a `while` over this."
            },
            {
              "name": "update",
              "signature": "() -> bool",
              "doc": "Applies what has been accumulated and starts a new group, for the group\na run ends in the middle of. Does nothing, and says so, if none is open.\n\nAcross a cut it costs one pass over the transposed stages and not a step:\nwhat travels is the fact that the group is over, in an empty envelope."
            }
          ],
          "properties": []
        }
      ],
      "functions": [
        {
          "name": "architecture",
          "signature": "(graph: Graph, example: Any = None, *, most: int = 48, depth: int = 0, broker: Broker | None = None) -> dict[str, Inside]",
          "doc": "What each node is made of, as `{node: Inside}` — ready for a figure.\n\n    g.figure(inside=architecture(g, x))\n\n**The graph is run once**, with hooks on everything every node holds. Not an\noptimisation: a node in the middle is handed what the nodes above produced,\nand tracing it on the graph's own input feeds a fan-in the wrong thing —\nwhich it did, until a picture with an empty box said so. Then, module by\nmodule, `torch.fx` is asked for the same thing; where it can answer it wins,\nand the seam is a module boundary rather than a judgement call.\n\n`example` is one input to run **the graph** on. A composite everybody\nrecognises is **one** box and is not opened; `depth=` opens them, and blocks\nthat are the same block collapse to one and a `×N`."
        },
        {
          "name": "kind_of",
          "signature": "(what: Any) -> str",
          "doc": "What kind of thing this is, by role and never by exact class name.\n\nA class this table has never heard of that ends in `Norm` is a\nnormalisation. Guessing by suffix is a guess and a good one — the\nalternative is calling half of everybody's models `other`."
        },
        {
          "name": "traced",
          "signature": "(module: Any, example: Any = None) -> Inside | None",
          "doc": "What this module is made of. `fx` if it can, a real forward if it cannot.\n\n`example` is an input to run it on; without one only the symbolic path is\navailable, and a module that needs a forward answers `None`."
        },
        {
          "name": "envelope",
          "signature": "(gradient: Any, closing: bool = False) -> Envelope",
          "doc": "A gradient in its envelope, which is how one crosses an edge. `closing`\nsays this gradient ends a group of accumulated ones, so whoever is\naccumulating applies what it has — it rides on the envelope because it is the\nsame fact seen from the other end."
        },
        {
          "name": "fedavg",
          "signature": "(exports: Iterable[Export], sizes: Sequence[float] | None = None) -> Export",
          "doc": "The average of what several training runs exported, weight for weight.\n`sizes` is how many samples each one saw, which is what FedAvg weights by;\nleft out, they weigh the same::\n\n    average = fedavg([client.export() for client in clients], sizes=[900, 100])\n\nWhat is **not** averaged is whatever is not a floating-point number: a\n`num_batches_tracked` is a count and the mean of two counts is not one. The\nfirst one's is kept, which every implementation does and none says out loud."
        },
        {
          "name": "freeze",
          "signature": "(graph: Graph, *node_ids: str) -> None",
          "doc": "Settles these nodes — or whatever was already declared `.frozen()`.\n\nWith ids it declares and obeys; with none it only obeys, which is what\n`Trainer` calls so a `.frozen()` in the expression is true before the first\nstep rather than after somebody notices."
        },
        {
          "name": "gather",
          "signature": "(store: Store, what: Any, *, run: str, round: int, clients: int, mine: int, size: float | None = None, within: float = 600.0, asking: float = 1.0) -> Any",
          "doc": "Puts this client's round in, waits for everybody else's, and gives back the\naverage. `run` names the training run, `round` which round, `clients` how\nmany there are and `mine` which one this is. `size` is how much data this\nclient saw, and it travels in the record so whoever averages can weigh by it.\nRaises `TimeoutError` after `within` seconds, naming who never turned up."
        },
        {
          "name": "gradient",
          "signature": "(value: Any, device: str | None = None) -> torch.Tensor | None",
          "doc": "What an envelope carries, as a tensor — or the sum of what a map of them\ncarries. `None` for something that is not a backward message at all, and\n`None` too for an envelope carrying nothing."
        },
        {
          "name": "parameters",
          "signature": "(graph: Graph, without: Container[str] = ()) -> list[torch.nn.Parameter]",
          "doc": "The parameters of every node in the graph that has any.\n\nIt asks for `.parameters()` and skips whoever lacks it, so a tokenizer does\nnot stop being a node for having nothing to train. Without repeats **by\nidentity** — two nodes can share a module — and in declaration order.\n\n`without` names the nodes to leave out — the ones somebody else updates::\n\n    trains = {\"body\": Split(SGD, lr=0.1)}\n    Adam(parameters(g, without=trains), lr=1e-3)\n\nLeaving them in is refused by the `Trainer`: a node trained where it runs and\nalso held by this optimizer is **updated twice** when where it runs is here."
        },
        {
          "name": "probe",
          "signature": "(graph: Graph, example: Any, *, depth: int = 0, most: int = 48, probes: int = 24, watching: Any = None, broker: Broker | None = None) -> dict[str, dict[str, Any]]",
          "doc": "What this graph looks like at initialisation, as `{where: numbers}`.\n\n`where` is a node, or `node.path.to.submodule` — the same keys the audit uses\nand the same scope the figure draws, so a finding from a probe lands on the\nbox a finding from a run would. The answer is the shape\n`somatize.health.seen` returns from a store. `watching=` takes a `Recorder`\nor anything callable."
        },
        {
          "name": "proxies",
          "signature": "(graph: Graph, example: Any, *, target: Any = None, objective: Objective | None = None) -> dict[str, float]",
          "doc": "Every proxy that can be taken with what it was given, as `{name: score}`.\n\nWithout a `target` and an `objective` the three that read a loss are **not in\nthe answer**, rather than being in it as `None`: a score that is missing and\na score that is bad have to look different."
        },
        {
          "name": "proxy",
          "signature": "(graph: Graph, example: Any, of: str, *, target: Any = None, objective: Objective | None = None) -> float",
          "doc": "One cheap score for one candidate, higher being better. `of` names which —\nsee `EVERY`. `snip` and `grasp` want a `target` and an `objective`.\n\nThe units are nobody's, and comparing two runs of the same proxy is the only\nthing it is for, which is why every one that spans decades comes back as a\nlogarithm."
        }
      ],
      "constants": []
    },
    {
      "name": "somatize.worker",
      "doc": "The generic worker: `pip install somatize` and nothing else.\n\n    python -m somatize.worker --listen 127.0.0.1:7000 [--store /scratch/soma]\n\nAn independent process on whatever machine. It starts **empty** — it does not\nknow what `tokenize` is — and waits for someone to connect and send it what to\nbuild its catalog from. There is no way of giving it one by hand, on purpose.\n\n| artifact kind | what arrives | what the worker supplies |\n|---|---|---|\n| `project` *(default)* | names, versions and state | **the code**, from its clone |\n| `pickle` | the code and the state | nothing |\n\n`project` is what you want when the worker runs in a clone of the project: tens\nof bytes per node, no coupling between interpreters, and it **checks the\nversion** — `--strict` *(default)* stops with both versions in front of you and\n`--lucky` runs anyway and says so on `stderr`.\n\n**Whoever reaches this port runs code here**, as their user. There is no\nauthentication and there is not going to be one — that is `srun`'s and `ssh`'s\njob. Bind to `127.0.0.1` and tunnel, or to a private interface inside a cluster.\n\nIt does not solve the **environment**: `cloudpickle` moves your objects, not\n`torch`. That belongs to whoever stands the worker up, and putting it in here\ncost the original 420 lines and a hot `pip install`. It fits in one file::\n\n    #!/usr/bin/env -S uv run --script\n    # /// script\n    # requires-python = \"==3.13.*\"\n    # dependencies = [\"somatize[remote]\", \"torch==2.10.0\"]\n    # ///\n    from somatize import worker\n    worker.listen(\"0.0.0.0:7000\", store=\"/scratch/soma\")\n\n`stdout` **is** the wire, so at startup `sys.stdout` is redirected to `stderr`: a\nstray `print()` in one of your nodes would otherwise break the protocol.",
      "classes": [
        {
          "name": "Pickles",
          "signature": "()",
          "doc": "Turns `pickle` artifacts into nodes. Python's `Provision`:\n`accepts(client, kind)` returns `None` or **how this worker identifies\nitself**, and `provide(kind, blob)` returns the nodes as a dict.",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "accepts",
              "signature": "(client: str, kind: str) -> str | None",
              "doc": ""
            },
            {
              "name": "provide",
              "signature": "(kind: str, blob: bytes) -> dict[str, Any]",
              "doc": ""
            }
          ],
          "properties": []
        },
        {
          "name": "Project",
          "signature": "(strict: bool = True)",
          "doc": "Opens `project` artifacts: resolves the classes against **this** clone.\nNo code comes over the wire, so neither `cloudpickle` nor matching\ninterpreters are needed.",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "accepts",
              "signature": "(client: str, kind: str) -> str | None",
              "doc": ""
            },
            {
              "name": "provide",
              "signature": "(kind: str, blob: bytes) -> dict[str, Any]",
              "doc": ""
            }
          ],
          "properties": []
        },
        {
          "name": "Provision",
          "signature": "(*args, **kwargs)",
          "doc": "How a worker turns an artifact into a catalog. A `Protocol` because there\nare three of these here and none inherits from the others. It is the seam\n`wire`'s `Provision` trait is filled from, and a user with a fourth way of\npacking nodes writes one and passes it to `listen`.",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "accepts",
              "signature": "(client: str, kind: str) -> str | None",
              "doc": "`None` to accept, or **how this worker identifies itself** so the\nclient can see what it disagrees with."
            },
            {
              "name": "provide",
              "signature": "(kind: str, blob: bytes) -> dict[str, Any]",
              "doc": "The nodes that artifact unpacks to, by id."
            }
          ],
          "properties": []
        },
        {
          "name": "Strategies",
          "signature": "(**by_kind: Provision)",
          "doc": "Several ways of building the catalog, asked by `kind` and not tried in a\nchain to see which sticks.",
          "exception": false,
          "constructors": [],
          "methods": [
            {
              "name": "accepts",
              "signature": "(client: str, kind: str) -> str | None",
              "doc": ""
            },
            {
              "name": "provide",
              "signature": "(kind: str, blob: bytes) -> dict[str, Any]",
              "doc": ""
            }
          ],
          "properties": []
        }
      ],
      "functions": [
        {
          "name": "listen",
          "signature": "(addr: str, provision: Provision | None = None, opened: Callable[[str], None] | None = None, store: Store | str | None = None, reporting: float | None = None) -> None",
          "doc": "Stands on `addr` and serves whoever connects. It does not return.\n\n`provision` says what the implementations are resolved with. `stdout` is not\ntouched — here the wire is the socket — and `opened` is called once with the\nreal address, so port `0` can be asked for. `store` answers two questions\nthat stay two: an artifact it already has is not sent again, and a node whose\nanswer is there is not run again."
        },
        {
          "name": "runtime",
          "signature": "() -> str",
          "doc": "How this process identifies itself, so a mismatch is refused on connect\ninstead of surfacing inside a `loads`. `somatize`'s version goes here, which\nis why it does not go into each class's fingerprint."
        },
        {
          "name": "serve_provisioned",
          "signature": "(provision: Provision | None = None, store: Store | str | None = None, reporting: float | None = None) -> None",
          "doc": "Serves slices with the catalog the client sends, until the client closes.\nWithout an argument it opens both kinds."
        }
      ],
      "constants": []
    }
  ]
}
