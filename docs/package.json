{
  "name": "docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "notebooks": "python3 scripts/notebooks_to_docs.py",
    "predev": "npm run notebooks",
    "prestart": "npm run notebooks",
    "prebuild": "npm run notebooks",
    "precheck": "npm run notebooks",
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "node scripts/check-sidebar.mjs && node scripts/check-anchors.mjs && node scripts/check-debt-refs.mjs && node scripts/gen-surface.mjs --check && astro build"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.38.2",
    "astro": "^6.0.1",
    "sharp": "^0.34.2"
  }
}
